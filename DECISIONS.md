# 결정 로그

이 프로젝트는 긴 설계 논의에서 나왔고, **여섯 번 크게 뒤집혔다.**
아래는 "이미 시도했다가 기각된 것"의 목록이다. 되돌리기 전에 읽을 것.

항목이 세 종류다:

- **D1~D6, D8~D11** — `기각된 안 → 채택된 안 → 뒤집힌 이유`. 되돌리기 전에 읽을 것.
- **D7** — 뒤집힌 결정이 아니라 **지우지 말아야 할 것**. 지금 놀고 있지만 되살아날
  조건이 분명해서 "안 쓰이니 지우자"는 판단이 반복될 자리다.
- **D12** — 결정이 아니라 **측정 기록**. 코퍼스가 커질 때 무엇이 먼저 깨지는지를
  미리 재둔 것이라, 그 규모에 닿기 전에 열어볼 자리다.

---

## D1. 경로 압축 → 폐기 (조건부 보류)

**기각:** Union-Find식 경로 압축으로 탐색 그래프를 최적화.

**이유의 변천:**
1. 처음엔 "의미적 유사성은 추이적이지 않다"로 기각했다.
   정규화 벡터에서 `cos(A,B)=cos(B,C)=0.8`이면 `cos(A,C)`의 하한이
   `2(0.8²)−1 = 0.28`이고 이 하한은 실제로 달성 가능하다(계산 확인).
2. **그 논거는 나중에 무효가 됐다.** 간선이 "유사하다"가 아니라 "이 경로가
   유용했다"만 주장하고, 읽기 전용이라 사람이 그 간선을 읽지 않는다.
3. **진짜 이유(2026-08-04 재검토로 교체):** **압축할 체인이 애초에 존재하지 않는다.**
   `postings` 는 `항 → 페이지 → (hits, misses)` 인 **이분 구조**지 페이지→페이지
   그래프가 아니다. Union-Find 경로 압축은 `parent[node] → node` 포인터 숲이 있어야
   성립하는데 그런 구조가 없다. 게다가 간선을 만드는 `apply()` 는 `dest`(= `reads.last()`)
   하나만 기록하므로, 세션이 A→B→C 를 읽어도 남는 것은 `항 → C` 하나다 —
   **기록 시점에 이미 최대로 압축돼 있다.**

   > 이 자리에는 원래 "앵커가 홉 수를 6.04→1.81로 줄여 압축할 게 안 남았다"가 적혀
   > 있었다. 그 수치는 무관한 두 실험 값을 이어붙인 오기였고(실제 6.04→3.33),
   > 더 근본적으로는 **범주 오류**였다: `n` 은 랭킹 리스트 스캔 깊이지 그래프 경로
   > 길이가 아니고, 그 `n−1` 을 아껴주는 것도 경로 압축이 아니라 L2 숏컷 자체다.
   > 정정된 값은 오히려 학습 레이어에 유리하다 — 손익분기 `p_wrong/(n−1)` 의 문턱이
   > n=1.81 대비 약 2.9배 낮아진다. 근거였던 합성 시뮬레이션(`bench/`)은 이후 제거됐다.

**되돌아오는 조건:** `n` 이 커지는 것과는 무관하다(그때도 압축할 그래프는 없다).
**중간 읽기를 별도 간선으로 기록하기 시작하면** 비로소 체인이 생겨 압축이 의미를 갖는다 —
다만 그것은 "압축이 아니라 가산" 원칙이 이미 금지하는 방향이다.

**부수 논점:** "압축 대 가산"은 알고리즘 차이가 아니라 구현 선택이다.
가법 Union-Find도 같은 상수 시간 이득을 준다. 다만 여기서는 가법이 **강제**인데,
목적지가 시작 노드만의 함수가 아니라 질의에 따라 달라지기 때문이다.

---

## D2. 손익분기 `1/n` → `p_hit > p_wrong/(n−1)`

**기각:** `E[L2] = p·1 + (1−p)(1+n)` → 손익분기 `p > 1/n`.

**오류:** 케이스를 둘로 뭉갰다. 실제로는 셋이고, **"숏컷이 없음"은 헛읽기 비용이
0인데** 그것을 실패에 포함시켰다.

```
적중        1 읽기      p_hit
잘못된 숏컷  1+n 읽기    p_wrong
숏컷 없음    n 읽기      p_none     ← 손해 없음

p_hit·1 + p_wrong(1+n) + p_none·n < n   ⟹   p_hit(n−1) > p_wrong
```

**함의:** 기준이 절대 적중률이 아니라 **적중 대 오답 비율**이다. 캐시가 확신 없을 때
침묵하면 `p_wrong → 0`이라 거의 항상 이득이다. EB 게이트가 하는 일이 그것이다.

**발견 경위:** 0% 워밍업에서 `E[L2] < n`이라는 물리적으로 불가능한 값이 나와 추적했다.
별개로 베이스라인 `n`을 고유 키 평균으로 계산한 버그도 있었다(Zipf 반복 무시).

---

## D3. Wilson 하한 → Empirical Bayes

**기각:** 신뢰도를 Wilson score 하한으로만 계산.

**이유:** Wilson은 카운트만 본다. 같은 4승 3패라도 검색이 강하게 가리키는 후보와
겨우 걸린 후보를 구분하지 못한다. **Wilson은 균등 사전(정보 없음)의 특수한 경우**이므로,
검색 점수를 사전분포로 주면 그 정보가 살아난다.

```
prior = Beta(s·κ, (1−s)·κ)      s = 정규화된 검색 점수
post  = Beta(s·κ + hits, (1−s)·κ + misses)
```

사전분포는 데이터가 없을 때만 일한다 — 400승 300패에서 격차가 0.005로 사라진다.
그래서 탐색을 위해 서빙 품질을 희생할 필요가 없다.

**함께 기각:** ε-randomization. 탐색을 위해 서빙을 희생하는 건 손실이다.
그리고 애초에 편향이 아니라 **표본 부족**이었다 — 적중률 자체는 순위와 무관하게
같았고, 웹 검색과 달리 여기서는 검토 여부가 관측 가능하므로 IPS도 불필요하다.

---

## D4. 서버 색인 → 클라이언트 분산 → 서버 색인 (두 번 뒤집힘)

**최종: 서버가 전체 코퍼스를 색인한다.**

클라이언트 분산 색인을 잠시 채택했던 이유는 ACL 회피였다. 기각한 이유:

| | 클라이언트 분산 | 서버 색인 |
|---|---|---|
| Confluence API 부하 | 사용자 수 비례 (**200명이면 200배**) | 1배 |
| dense 임베딩 | 중복 계산 | 1회 |
| IDF | 권한 좁은 사용자는 추정 붕괴 | 전역 안정 |
| **랭킹 척도** | **사용자마다 다름** | 동일 |
| 보고서(고아·누락 링크) | 개인 뷰라 무의미 | 전역 그래프 |

넷째 줄이 결정적이다. 척도가 다르면 EB 사전분포로 들어오는 점수가 서로 달라
**이질적인 랭커의 관측을 하나의 카운터에 섞게** 된다.

"서버측 코퍼스가 ACL 문제의 원천 전부"라고 했던 건 과장이었다. 카운터만 둬도
질의 키워드는 서버로 가고, 질의 시점 ACL은 엔터프라이즈 검색의 표준이다.

---

## D5. 볼트 배포 → 서버 서빙

**기각:** 서버가 ACL 필터링한 볼트를 클라이언트에 배포.

**이유:** **배포된 사본은 회수할 수 없다.** 사용자가 팀에서 나가도 노트북의 볼트에는
전체 사본이 남는다. 오프라인이면 삭제 명령이 도달조차 못 한다.
권한 시행에 그토록 공을 들였는데 배포가 그것을 무효화한다.

**연쇄 단순화:** 서버가 읽기를 서빙하면 **훅이 통째로 불필요해진다** — 서버가 궤적을
직접 관측하므로 클라이언트 버퍼링·핫패스 비용·세션 조립이 사라진다.
플러그인이 `스킬+훅`에서 **MCP 도구 4개**(`search`/`read`/`grep`/`tree`)로 바뀐다.

**함께 정정:** "서버판 = 로컬판 + 서버"가 아니다. 코퍼스 위치·인터페이스·ACL 방식이
전부 다르므로 **증분이 아니라 전환**이다.

---

## D6. 궤적 보고 → 관측

**기각:** 에이전트가 `record_trajectory` 도구를 호출해 궤적을 보고.

**이유:** 에이전트는 "작업 보고" 성격의 도구를 안정적으로 호출하지 않는다.
L2 전체가 그 호출에 의존하면 데이터가 안 쌓인다.

**중간 안(로컬 볼트 + 훅):** Claude Code 훅이 네이티브 `Read`를 관측할 수 있다.
페이로드에 `session_id`가 있어 궤적 조립이 된다. **이것도 D5로 폐기됐다** —
서버가 읽기를 서빙하면 훅 없이 직접 관측된다.

**교훈:** "가능하다"와 "그래야 한다"는 다르다. 훅은 로컬 볼트에 대한 *반대 논거를
제거*했을 뿐인데 한동안 *찬성 논거*로 읽었다.

---

## D7. `mirror/structure/` 유지 (지금은 아무도 안 읽는데도)

**현황:** `build`가 페이지마다 구조 서명(제목·헤딩·링크)을 `mirror/structure/` 에 쓰지만
**읽는 코드가 한 곳도 없다.** Kotlin 서버는 `structure` 라는 단어조차 없다.

**만든 이유:** 무효화 대상을 좁히려는 것이었다. 산문만 고친 수정은 `pages/` 만 바꾸고
이 파일은 그대로여야 하므로, `git diff -- structure/` 한 줄이 "구조가 실제로 바뀐
페이지" 목록이 된다. `canonical_json` 의 결정적 직렬화와 `to_dict()` 의 링크 정렬이
전부 이걸 위해 존재한다 — diff 가능한 스냅샷이어야 하기 때문이다.

**왜 놀고 있나:** 무효화 메커니즘 자체가 구현된 적이 없고, 더 근본적으로 "증분 색인을
안 한다"는 결정이 전제를 없앴다. 전체 재구축이 수 초면 무엇이 바뀌었는지 알 필요가 없다.

**그런데 지우지 않는다.** 코퍼스가 커지면 되살아나는데, 그 이유가 처음 의도와 다르다:

- **변경 감지 용도로는 이미 대체됐다.** 어떤 페이지가 바뀌었는지는 `.sync-state.json` 의
  `version` 이 알려주고 `sync` 가 그걸로 증분 페치를 한다. 더 싸고 이미 있다.
  구조가 바뀌었는지 알려면 파싱해야 하므로, 파싱을 피하려고 structure 를 보는 건 순환이다.
- **남는 역할은 과잉 무효화 방지다.** `version` 은 너무 거칠다 — 실측(Acme 2,378건)에서
  80%가 한 번 이상 수정됐고 중앙값 v4, 최대 v145다. 오탈자 하나에도 버전이 오르므로
  version 기준 무효화는 학습 간선을 거의 전량 날린다. `structure/` 는 "링크·헤딩이
  실제로 바뀐 경우"만 골라낸다.

**되돌아오는 조건:** 증분 build 를 도입할 때. 실측(2026-08-06)하면 전량 build 가
2,377건에 29.9초라 10만이면 **21분**이다(D12) — 그 규모에서는 전량 재파싱이
성립하지 않고, 증분이 들어오는 순간 "어떤 무효화가 정당한가"가 곧바로 문제가 된다.
**증분 build 와 `structure/` 는 한 세트다.**

**유지 비용이 거의 없다는 것도 근거다.** 파일당 평균 362B(10만이면 40MB, 100만이면
0.4GB), 2,378개 읽기가 59ms. `_write_if_changed` 덕에 실제 쓰기는 변경분만 일어난다.
지금 지웠다가 나중에 되살리면 diff 의 기준점이 될 이력이 없어 처음부터 다시 쌓아야 한다.

> 단, `headings` 는 추출만 하고 아무도 안 쓴다. 링크는 `anchors.jsonl` 을 뒤집으면
> 복원되므로(미해결 링크 72건 제외) `structure/` 고유 정보는 실질적으로 그 둘뿐이다.
> 증분 build 를 실제로 만들 때 이 스키마가 그대로 필요한지 재검토할 것.

---

## D8. 로컬판을 배포 가능하게 (볼트 고정 위치 + 스킬 절대경로화)

**문제:** 로컬판은 **구조적으로 배포가 불가능**했다. 스킬이 전부 cwd 상대경로
(`Grep(path="ALIASES.md")`)라 **작업 디렉터리가 볼트일 때만** 동작하는데, 볼트는
어느 프로젝트도 아닌 곳에 있다. 전역 설치하면 볼트 아닌 프로젝트에서 발동해 없는
파일을 grep한다 — description 이 공격적이라 실제로 발동한다.

**채택:** 볼트 경로를 `~/.wikilens/config.json` 에 기록하고, 스킬이 매번
`scripts/vault_status.py` 로 경로와 상태를 받아 모든 Grep/Read 앞에 붙인다.
설정은 `/wikilens-local:setup` 커맨드가 안내한다.

**볼트 배포 금지 계약과 충돌하지 않는다.** 배포하는 것은 스킬과 스크립트뿐이고
볼트는 **각자 자기 토큰으로 만든다**. 오히려 ACL 이 설계상 해결된다 — 볼 수 없는
문서는 애초에 볼트에 안 들어온다(서버판이 지금 막혀 있는 그 문제). 대가는 Confluence
부하가 사용자 수에 비례하는 것으로, D4 에서 서버 색인을 택한 이유와 같은 트레이드오프다.
**로컬판 배포는 ACL 을 부하와 맞바꾸는 선택**이라, 소규모 팀에는 유효하고 대규모에는
서버판이 맞다. 상한이 실측돼 있다 — 10만 문서면 `ALIASES.md` 흔한 말 매치가
5,687줄이 되어 컨텍스트가 범람한다(D12). 부하가 아니라 **여기서 먼저 못 쓰게 된다.**

**기각한 대안:**

- **환경변수만으로 볼트 지정** — Claude Code 가 띄우는 셸에서 `export` 해도 다음
  세션엔 없다. "어디서든 검색"이 세션마다 깨진다. `WIKILENS_VAULT` 는 일회성 재정의로만 남김.
- **setup 을 스킬로** — 스킬은 description 매칭으로 발동해서 "볼트가 없을 때만"을
  표현할 수단이 없고, 검색 스킬과 같은 발화에서 경쟁한다. 사용자가 명시적으로 부르는
  동작이므로 커맨드가 맞다.
- **CLI 를 플러그인에 동봉** — 설치는 **버전별 캐시로 복사**되고 구버전은 청소되므로
  `pip install -e ${CLAUDE_PLUGIN_ROOT}/cli` 는 캐시가 지워질 때 죽는다. 게다가
  `marketplace/plugins/` 수동 사본 6개를 없앤 것과 같은 실수를 CLI 에서 반복하는 셈이다.
  대신 `known_marketplaces.json` 의 `installLocation` 에서 CLI 소스를 찾는다.
- **로컬판도 MCP 로 통일** — 로컬판의 정의적 성질이 "검색 경로 런타임 의존성 0"이다.
  MCP 를 붙이면 첫 검색 전에 프로세스 기동이 필요해지고 파이썬 환경 문제가 검색 실패로
  나타난다. 두 판이 **다른 배포 형태**라는 D5 의 구분도 흐려진다.
  다만 MCP 는 권한 프롬프트와 경로 해석을 공짜로 푼다 — **실사용에서 권한 마찰이
  못 견딜 수준이면 그때 전환한다.** 전환 비용은 낮다(서버판 프록시와 그 테스트
  하네스가 이미 있다).
- **볼트를 플러그인에 넣어 배포** — 계약 위반이고, 관리자 권한 범위의 코퍼스가
  통째로 유출된다. 크기(59MB)는 부차적이다.
- **필요할 때만 조금씩 받기** — 원리적으로 불가. `build.py` 가 3패스라 전체 제목→ID
  색인이 있어야 링크가 해석되고, 앵커 전치는 모든 문서의 링크를 봐야 한다.

**실측으로 확인한 것:** 플러그인 설치는 소스 편집을 **자동 반영하지 않는다**(캐시 복사).
볼트 읽기는 프로젝트마다 승인이 필요하다(이 저장소 `settings.local.json` 에
`Read(//Users/…/wiki/**)` 흔적). 두 판의 SKILL.md 가 `name: wikilens` 로 같고
description 이 거의 동일해 동시 설치 시 오선택 위험이 있었다 — 양쪽 description 을
서로 구별되게 고쳤고 `shared_contract.sh` 가 동일해지는 것을 막는다. 그때 함께 적은
"둘은 배타적" 은 **나중에 강제 불가로 판명돼 우선순위로 대체했다 — D13.**

**아직 검증 못 한 것:** 이 저장소에 git remote 가 없어 `pip install git+URL` 분기,
타 머신 첫 설치, git source 마켓플레이스는 미검증이다. 다만 같은 머신에서
`acme-dt` 가 `https://github.com/example-org/dt-monitoring.git` 에서 설치돼
동작 중이므로 git source 경로 자체는 실재한다 — wikilens 로 확인만 안 됐을 뿐이다.

---

## D9. 플러그인 이름은 불변, 바꾸려면 `renames` 에 적는다

**문제:** 2026-08-05 에 플러그인 이름을 바꿨다. 서버판이 무표지 `wikilens` 를 차지해
"기본"을 참칭했는데 실제 배포 가능한 것은 로컬판이라, `wikilens-client` /
`wikilens-local` 로 갈랐다(디렉터리도 `plugin/client/` — 저장소의 `server/` 가 진짜
Kotlin 서버라 같은 단어가 두 물건을 가리켰다). 마켓플레이스도 `wikilens-tools` →
`wikilens`.

**이번엔 공짜였다. 다음엔 아니다.** 공식 마켓플레이스 README 가 명시한다 — 플러그인
`name` 은 **불변 슬러그**이고, 게시 후 바꾸면 사용자 설치가 `plugin-not-found` 로
깨진다. 아직 아무 데도 게시하지 않아 아무 일도 없었지만, 비공개 git 에 올리는 순간부터
같은 제약이 걸린다.

**채택:** 매니페스트 최상위에 `renames` 맵을 둔다. 로더가 이걸 읽어 옛 슬러그를 새
슬러그로 다시 써서 기존 설치를 자동 이전시킨다.

```json
"renames": { "wikilens": "wikilens-client" }
```

지금 이 한 줄은 **기능적으로 불필요하다** — 구 이름으로 설치한 사람이 없다.
남겨두는 이유는 다음에 이름을 바꿀 사람에게 여기 적으라고 알리는 것이다.
`shared_contract.sh` 가 대상이 실재하는지, 살아 있는 이름을 키로 쓰지 않았는지 본다
(둘 다 틀리면 조용히 아무 일도 안 한다 — 이전이 안 되거나 플러그인이 자기 자신에서
밀려난다).

**공식 디렉터리 등재는 지금 대상이 아니다.** PR 이 아니라 심사 제출이고
(`clau.de/plugin-directory-submission`), 문서가 전부 한국어에 게이트웨이 우회가
절차에 박혀 있으며, 무엇보다 **서버판이 ACL 수집 전이라 다중 사용자에 안전하지 않다.**
팀 배포 목적이면 비공개 git 마켓플레이스가 맞다.

---

## D10. 설정 정본을 `~/.wikilens/` 로 (양판 공통)

**문제:** 자격증명과 서버 주소가 **환경변수 전용**이었다. `export` 는 그 셸에서만
산다. 결과가 판마다 다르게 나타났다:

- 로컬판 — 검색은 되는데 `sync` 만 죽는다. 사용자는 정상인 줄 알고 갱신을 자기
  터미널에서 수동으로 하고 있었다. `/wikilens-local:sync` 는 **한 번도 동작한 적이 없었다.**
- 서버판 — Claude Code 를 앱으로 띄우면 환경이 비어 서버 주소가 조용히 localhost
  기본값이 되고 식별자가 없어 **모든 검색이 빈 결과**가 된다.

볼트 경로는 같은 이유로 이미 `config.json` 으로 옮겨져 있었다(D8). **자격증명만
그 교훈에서 빠져 있었다.**

**채택:** 정본은 `~/.wikilens/` 하나이고, 안에서 둘로 나눈다.

| | 무엇 | 왜 |
|---|---|---|
| `config.json` | 볼트 경로 · CLI 경로 · 서버 주소 · 사용자 식별자 | 비밀이 아니다 |
| `env.sh` (600) | Confluence 토큰류 | 비밀이다. **JSON 이 아니라 셸 스크립트** — 사용자가 자기 터미널에서 `source ~/.wikilens/env.sh` 로 그대로 재사용한다 |

환경변수는 남기되 **일회성 재정의**로만 이긴다.

**CLI 경로도 여기 기록한다.** venv·pipx 에 설치하면 그 셸을 활성화하지 않는 한 PATH
에도 없고 기본 python 으로 import 도 안 되는데, Claude Code 가 띄우는 셸이 정확히
그 상태다(실측: `cli/.venv` 에 설치했더니 래퍼가 못 찾았다). 해석처는
`vault_status.py` 하나이고 래퍼는 `--cli` 로 물어본다 — 둘이 각자 찾으면 스킬은
"CLI 있음"이라 하는데 래퍼는 못 찾는 상태가 생긴다.

**기각한 대안:** macOS 키체인(첫 조회 시 GUI 승인 팝업이 백그라운드 셸을 멈출 수
있고 macOS 전용), `config.json` 에 토큰까지(셸에서 재사용하려면 파싱이 필요해
수동 싱크 경로가 불편해진다).

**검증:** PATH 를 `/usr/bin:/bin` 으로 좁힌 셸에서 래퍼가 CLI 에 도달하고 실제
Confluence 인증까지 통과하는 것을 확인했다(2026-08-05). 로컬판 싱크 경로가 Claude
Code 안에서 처음으로 끝까지 동작한 시점이다.

---

## D11. 샤딩은 페이지 ID 의 **뒤** 2자리, 1단계

**문제:** 앞 4자리를 2단계로 쪼개고 있었다. 근거는 "10k 페이지면 리프당 1개 미만"
이었는데 **균등 분포를 가정한 계산**이었다. 실제로는 10,000개 리프 중 52개만 쓰고
한 디렉터리에 최대 378개가 몰렸다.

**측정:** Acme 2,377건 (2026-08-06)

| | 디렉터리 | 리프 최대 | 평균 |
|---|---|---|---|
| 앞2/앞4 (구) | 52 | 378 | 45.7 |
| 뒤2/뒤4 | 2,170 | 3 | 1.1 |
| **뒤2 (채택)** | **100** | **37** | **23.8** |

구 규칙은 2단계를 쓰고도 466→378, 18% 줄었을 뿐이다. 뒤에서 쪼개면 1단계로 열 배 낫다.
`뒤2/뒤4` 는 파일 2,377개에 디렉터리 2,170개라 과하다 — 그래서 depth 를 1로 줄였다.

**"몇 개부터 문제인가"는 재보니 거의 무관했다.** 구 주석의 "수천 개"에는 근거가
없었다. 실측(APFS): 한 디렉터리에 100개일 때 직접 열기 1000회가 23.5ms, **50,000개일
때 37.8ms** 다. 500배를 넣어도 개당 0.024→0.038ms 이고, 코드는 `ALIASES.md` 에 적힌
경로로 **직접 열지 나열하지 않는다**. 선형으로 느려지는 건 나열뿐이다(0.1ms → 25.3ms).

그래서 샤딩의 진짜 수혜자는 이 코드가 아니라 **나열하는 쪽**이다 — git·Finder·rsync·
편집기(Obsidian 으로 볼트를 여는 사용이 실제로 있다), 그리고 APFS 만큼 관대하지 않은
파일시스템. 비용이 0 이라 보험으로 유지하지만, **문서량 때문에 depth 를 올릴 일은
사실상 없다**(100만 문서 → 디렉터리당 1만 개, 위 실측 범위 안).

코퍼스가 커질 때 실제로 먼저 깨지는 것은 따로 있다 — **D12** 에 측정해 뒀다.

**왜 앞자리에 엔트로피가 없나 — 처음엔 추론이었다가 확인했다.**

"시간순 연속 할당"이라고 단정해 커밋했는데 근거를 요구받고 실제로 쟀다:

- 자릿수별 엔트로피 1번째 1.93 bit vs 9번째 3.32 bit (10값 균등이 3.32)
- 인접 ID 간격 중앙값 13, 46%가 10 이하. 폭 3.3억에 2,377개가 균등이었다면
  중앙값이 약 138,000 — 네 자릿수 차이다
- REST API 로 표본 31개의 `history.createdDate` 를 받아 대조: **스피어만 ρ=0.9964**,
  역순 465쌍 중 6쌍(1.3%), 2022-09~2026-08

메커니즘은 **Hi/Lo** 다. Confluence 는 `hibernate_unique_key` 의 `next_hi` 로 ID 를
만든다 — DB 에서 high 를 받아 메모리에서 low 를 채우고 소진되면 다시 받는다. 관측이
그대로 설명된다: low 안은 연속(간격 13), high 가 건너뛰며 큰 구멍(폭 3.3억), high 가
느리니 앞자리 엔트로피가 낮고 low 가 블록을 채우니 뒷자리가 최대다. Confluence 공통
구현이므로 다른 인스턴스도 같은 분포를 기대할 수 있다.

**대가:** 온디스크 레이아웃을 깨는 변경이다. 이 머신에만 볼트가 있어 지금은 공짜였고
(7,131개 파일을 md5 대조하며 이전), 비공개 git 에 올린 뒤라면 이전 경로를 제공해야 한다.
규칙이 Python·Kotlin·플러그인 3곳에 복제돼 있어 셋을 함께 옮겼다.

---

## D12. 10만 문서에서 먼저 깨지는 것 (샤딩이 아니다)

**결정이 아니라 측정 기록이다.** "코퍼스가 커지면 샤딩 depth 를 올린다"는 계획이
D11 실측으로 근거를 잃었으므로, 그 자리에 **실제로 먼저 깨지는 것**을 적어둔다.
10만에 닿기 전에 이 문서를 다시 열 사람을 위한 것이다.

**측정** (Acme 2,377건, 2026-08-06. 10만 환산 배율 42.1)

> `build` 는 그 뒤 한 번 내려갔다 — 마크다운 변환이 소프를 직렬화했다가 되파싱하던
> 왕복을 없앴다(2,383건 기준 42.4초 → 28.3초). **표의 29.9초는 그 이전 값이다.**
> 그래도 10만이면 20분이라 이 절의 결론은 그대로다 — 증분 build 가 답이지
> 상수배 최적화가 답이 아니다.

| | 현재 | 10만 | 성격 |
|---|---|---|---|
| 샤딩 디렉터리당 | 24 | 1,000 | **무관** — 5만개 넣어도 개당 0.038ms (D11) |
| `ALIASES.md` grep | 80ms | 80ms | 속도는 무관 |
| └ **흔한 말 매치** | ~130줄 | **5,687줄** | **컨텍스트 범람 — 로컬판이 여기서 죽는다** |
| `build` 전량 재파싱 | ~~29.9초~~ → 28.3초 | ~~21분~~ → **20분** | 증분 없음, 싱크마다 |
| 서버 재색인 | 6.7초 | 5분 | 기동 시마다 (2026-08-06 부터) |
| 볼트 디스크 | 40MB | 1.7GB | 로컬판은 **1인당** |

**깨지는 순서**

1. **로컬판이 가장 먼저, 그리고 완전히 죽는다.** 스킬은
   `Grep(pattern, path="<VAULT>/ALIASES.md", output_mode="content")` 로 시작하는데,
   매치 5,687줄이 통째로 컨텍스트에 들어간다(줄당 80자면 450KB). 검색이 느려지는 게
   아니라 **답을 못 한다.** 지금은 흔한 말도 130줄이라 보이지 않는 문제다.
   서버판은 BM25 가 상위 N 개만 주므로 애초에 이 문제가 없다 — **10만은 서버판의 규모다.**
2. **`build` 21분.** 여기가 `mirror/structure/` 가 되살아나는 자리다(D7). 무엇이
   실제로 바뀌었는지 골라내지 못하면 싱크마다 21분을 낸다.
3. **서버 기동 5분.** 오늘 넣은 기동 적재(D-없음, `vaultBootstrap`)가 2,377건에선
   6.7초라 공짜지만 그 규모에선 아니다. **ACL 만 적재하고 색인은 디스크 것을 쓰는
   분기**가 필요해진다 — 지금 전체 재구축을 하는 이유는 "서버가 내려간 사이 sync 가
   돌았을 수 있다" 하나뿐이므로, 그 조건을 따로 감지하면 된다.

4. **서버 `grep` 이 요청마다 전량 스캔**. Lucene 이 본문을 저장하지 않으므로
   (`Field.Store.NO`) `read`·`grep` 은 매 요청 디스크를 읽는다 — 색인만으로는 못 푼다.
   실측: 2,383 문서 전체 스캔이 JVM 0.64초, ripgrep 0.01초 미만(**65배**). 10만이면
   JVM 은 27초다.

   > **10만을 기다릴 것도 없었다.** 실제 코퍼스 13,921건에서 단일 grep 이 **2.44초**다
   > (실측 2026-08-08). `GREP_BUDGET_NANOS` 가 **3초**이므로 여유가 0.56초뿐이고,
   > 코퍼스가 **17,000건쯤에서 예산에 닿는다.** 그때부터 `truncated=true` 가 상시화되고
   > **뒤쪽 문서는 영영 안 읽힌다** — 실패가 아니라 조용한 부분 응답이라 사용자는
   > "없다"로 받는다. 그래서 (d) 를 실행했다 — D20.
   >
   > **그리고 위의 "65배" 는 이 맥락에서 틀린 수치다.** 그건 2,383건 시절 rg 를 셸에서
   > 맨몸으로 돌린 값이다. 서버 안에 넣고 같은 코퍼스로 재니 **4~6배**다(D20 의 표) —
   > rg 가 못 없애는 고정비가 있기 때문이다. 13,921개 메타에 ACL 을 걸어 대상 목록을
   > 만드는 일은 **두 엔진이 똑같이** 한다. 이 수치로 결정하지 말 것.

**그래서 10만에서 할 일은 샤딩이 아니라 이 넷이다:** (a) 증분 build, (b) 기동 시
조건부 적재, (c) 로컬판을 그 규모에 쓰지 않는 것, (d) `grep` 을 ripgrep 으로.

**(d) 는 절반만 했다 — 엔진은 바꿨고 프로세스는 안 바꿨다.** rg 를 원한 이유가 둘인데
따로 떼어낼 수 있었다:

- **성질**(유한 오토마타 → ReDoS 없음) — `com.google.re2j` 로 해결했다. ripgrep 이
  쓰는 것과 같은 RE2 알고리즘을 JVM 안에서 돌린다. jar 단독 배포가 유지된다.
- **속도**(65배) — 이건 여전히 프로세스 밖에 있다. 안 했다.

지금 규모에서 필요한 것은 성질이었다. 실측(2,383 문서 전량): `(.+)+@@@@` 가
**끝나지 않던 것에서 1.62초 전량 스캔**(잘림 없음)이 됐고, 스택을 넘겨 HTTP 500 이던
`(a|aa)+c` 도 정상 응답이다. 정상 패턴의 매칭 비용은 16.4만 줄에서 JVM 대비
0.38~3.37배(9~49ms)로 **전체 스캔 0.64초를 I/O 가 지배해 묻힌다** — 세 패턴 모두
매치 건수는 동일했다.

**rg 바이너리를 지금 안 붙이는 이유는 그대로다.** 배포가 jar 하나에서 "rg 를 먼저
깔아라" 가 되고, 없으면 `grep` 이 죽는다(이 개발 머신에도 rg 바이너리가 없다 —
`rg` 는 Claude Code 가 `ARGV0=rg` 로 자기 자신을 재실행하는 셸 함수다). ACL 로 거른
경로 목록을 넘기는 것도 자명하지 않다 — rg 는 stdin 을 파일 목록이 아니라 **검색 대상
텍스트**로 읽으므로 인자로 넘겨야 하고, 10만이면 `ARG_MAX` 에 걸려 배치로 쪼개야 한다.

**나중에 붙이기는 쉬워졌다.** 두 경로가 같은 RE2 문법이라 답이 갈리지 않는다. 전에는
JVM 폴백과 rg 가 문법이 달라(역참조·전방탐색) 같은 질의가 두 답을 낼 수 있었고,
그건 이 프로젝트가 "조용히 실패하는 것" 으로 분류하는 자리다.

대가는 **역참조와 전방탐색을 못 쓰는 것**이다. ripgrep 도 같은 이유로 못 쓴다. 다만
조용히 0건이 되지 않도록 `GrepResponse.error` 로 이유를 돌려준다 — 정규식 문법 오류는
ACL 과 달리 코퍼스에 대해 아무것도 알려주지 않으므로 숨길 이유가 없다.

## D13. 두 판은 배타적이 아니라 **우선순위**다 (서버판 우선)

한때 "둘 중 하나만 설치하세요" 라고 두 곳에 적었다 — 마켓플레이스 설명과 각
`SKILL.md` 의 description. **강제하는 코드는 없었고, 강제할 수도 없었다.**

**실측으로 드러난 것:** 이 개발 머신의 `~/.claude/settings.json` 이
`{"wikilens-local": true, "wikilens-client": true}` 였다. 그 경고를 쓴 사람이 둘 다
켜둔 채 아무 신호도 못 받았다. 게다가 서버판이 켜져 있으면 그 MCP 도구 4개는
**스킬 선택과 무관하게 항상** 모델에게 노출된다(같은 세션에서 확인). 그래서
"로컬판이 이긴다" 는 규칙은 원리적으로 성립할 수 없다 — 서버판 도구를 숨길 방법이 없다.

또 하나: 그 경고가 읽히는 시점이 틀렸다. "설치하지 마세요" 는 **설치 시점 조언**인데,
`SKILL.md` 의 description 은 이미 둘 다 깔린 뒤 **선택 시점**에 읽힌다. 그때는 늦었고
모델이 할 수 있는 일도 없다.

**그래서 우선순위로 바꿨다.** 서버판이 보이면 서버판, 안 보이거나 서버가 응답하지
않으면 로컬판. 이건 모델이 **도구 목록만 보고 즉시 판정**할 수 있어 코드가 0줄이고,
방향도 맞다 — 서버가 응답하면 서버판이 엄밀히 더 낫다(BM25+Nori+앵커+학습+계층).
배타성을 못 지키는 것이 아니라 **저하가 우아해지는 것**이다.

정리하고 싶은 사용자를 위해 `vault_status.py` 가 `OTHER=` 로 상대편이 켜져 있음을
알리고 `claude plugin disable <이름>` 을 안내한다(끄는 것은 사용자 몫이다).

### 기각: 플러그인 하나로 합치기

매력적으로 보였다. 두 `SKILL.md` 가 **실제로 드리프트했고**(서버판에 "여러 낱말을
그대로 grep 하지 마세요" 가 없었다 — 로컬판이 실측으로 배운 것인데), 그게 중복의
실제 비용이다. 합치면 배타성 문제가 아예 사라진다.

**그런데 `.mcp.json` 이 딸려간다.** 플러그인에 선언된 MCP 서버는 조건부로 안 뜨므로,
로컬판만 쓰는 사용자에게도 파이썬 프로세스가 뜬다. D8 이 정의한 로컬판의 성질이
**"검색 경로 런타임 의존성 0"** 이고, 깨지면 파이썬 환경 문제가 검색 실패로 나타난다.
D8 이 피한 것이 정확히 그것이다. 중복 비용은 계약으로 갚는다 — 두 스킬이 같은
우선순위를 말하는지, 대소문자를 같이 무시하는지를 `shared_contract.sh` 가 검사한다.

---

## D14. 분석기는 색인이 정한다 — 질의 언어로 고르지 않는다

`--wikilens.analyzer=korean|english|standard` 는 **재색인할 때 무엇으로 지을지**만
정한다. 질의는 언제나 디스크 색인이 실제로 지어진 분석기를 쓴다(커밋 데이터에 기록).

**한 번 뒤집혔다.** 처음엔 설정을 질의에도 쓰고 색인 기록과 **대조해 ERROR 를 찍었다.
색인에 답이 적혀 있는데 안 쓰고 어긋남을 재고만 있었던 셈**이라, 재색인 전까지 검색이
조용히 0건이었다. 기록을 그대로 쓰니 그 상태가 아예 성립하지 않는다.

### 기각: 요청의 질의 언어로 동적 선택

매력적으로 들리지만 **개선이 아니라 고장이다.** 색인은 이미 지어진 토큰을 갖고 있어서,
질의만 다른 분석기로 돌리면 없는 토큰을 찾는다. 실측:

```
색인=[production, servers]   영어분석질의=[product, server]   겹침 없음
색인=[deploying, pipelines]  영어분석질의=[deploi, pipelin]   겹침 없음
```

"영어 질의를 영어답게" 가 아니라 **0건**이다. 방금 없앤 결함을 요청 단위로 되살린다.

판정 자체도 성립하지 않는다 — 이 코퍼스의 실제 질의는 섞여 있다.
`Jenkins 배포 파이프라인` 은 한글 7 · 영문 7 로 **동률**이고, `DEPLOY_TOKEN 어디` 는
영문 11 · 한글 2 라 english 로 판정되지만 의도는 한국어 위키 조회다.

그리고 **학습이 흩어진다.** 포스팅 키가 분석된 항이라, 같은 질문이 요청마다 다르게
판정되면 카운트가 갈린다 — 조용히 실패하는 것들 2번과 같은 실패다.

### 되는 형태: 언어별 필드 (지금은 안 한다)

동적 선택을 질의가 아니라 **색인**에 두면 된다. 본문을 `body_ko`(Nori)·`body_en`(English)
로 두 번 색인하고 질의는 양쪽을 다 검색한다 — 그러면 **언어 판정이 필요 없다.**
맞는 쪽이 알아서 점수를 낸다. `PerFieldAnalyzerWrapper` 와 `MultiFieldQueryParser` 가
이미 있어 배선은 어렵지 않고, 대가는 색인 크기 약 2배다(현재 3.0MB / 본문 16MB).

**지금 안 하는 이유는 이 코퍼스에서 이득이 거의 없어서다.** 영문 굴절형이
267,346건 중 39,660건(15%)인데 상위가 `https`(11,408) · `status`(4,718) ·
`params`(1,033) 처럼 **굴절이 아니라 그냥 그 단어**다 — 어간 추출이 오히려
`status → statu` 로 망가뜨린다. 실제 이득은 `matching`·`pages` 정도다.

흐름을 그리다 별개의 틈도 하나 나왔다 — `SearchService` 가 항 추출과 검색을 위해
스냅샷을 **두 번 따로** 읽고 있었다. 사이에 재색인이 끝나면 항은 옛 분석기 것이고
결과는 새 색인 것이 된다. 검색 결과 자체는 맞아서 조용한데, **그 항이 학습 포스팅의
키**라 같은 질의가 그 순간에만 다른 키로 기록된다(실측: `[production, servers]` 대
`[product, server]`, 결과는 둘 다 1건). `analyzeAndSearch` 로 한 번에 받게 해서
그 상태가 성립하지 않게 했다.

**뒤집히는 조건은 "굴절형 비율" 이 아니라 "영어 문서 비율" 이다.** 한·영 문서가
섞인 위키(대략 영어 문서 20% 이상)를 만나면 그때 언어별 필드로 간다. 단일 언어
코퍼스면 `--wikilens.analyzer` 하나로 충분하고, 그게 지금 상태다.

## D15. 설정 절차를 줄이는 방법은 "안내를 잘 쓰기"가 아니라 **답을 하나로 정하기**다

**문제:** 로컬판 설정이 명령 12개 · 분기 3곳이었다. 분기를 하나씩 보면 전부 정당해
보이는데, 셋 다 **같은 병 하나**의 증상이었다 — 어떤 값이 어디 있는지 미리 정해두지
않아서, 정해두는 대신 **찾아내는 장치**를 붙였다.

| 정하지 않은 것 | 그 자리에 붙어 있던 장치 |
|---|---|
| CLI 가 어디 설치되는가 | `CLI_SOURCE` · `--cli-path auto` · `discover_cli()` · `config.json` 의 `cli` · 문서 세 곳의 "설치했는데도 못 찾음" · 트러블슈팅 1행 |
| 볼트 경로를 누가 CLI 에 전달하는가 | 호출자마다 `--root <VAULT>` 조립 + **"서브커맨드 앞에 와야 한다"는 함정 경고 4곳** |
| 서버가 볼트를 어떻게 아는가 | `server/mirror-root` 심링크를 손으로(그 안내는 `server/README.md` 에만 있었다) |
| 서버판 사용자가 설정을 어떻게 쓰는가 | JSON 손으로 작성 — 오타나면 조용히 localhost 기본값 |

**채택:** 각 물음에 답을 하나씩 정했다.

- **CLI 는 `~/.wikilens/venv` 에 설치한다.** 설치 뒤에 찾는 단계가 아예 없어진다.
- **`--root` 는 래퍼가 채운다.** 정본(`config.json`)을 아는 자리가 이미 래퍼이므로
  거기서 채우면 조립이 복제되지 않는다. 사용자가 직접 준 값은 그대로 둔다.
- **CLI 는 `--root` 를 서브커맨드 뒤에서도 받는다.** 함정을 경고하는 것보다 함정을
  없애는 편이 싸다(문서 4곳 → 0곳).
- **서버는 기본 자리가 비면 `~/.wikilens/config.json` 의 `vault` 로 폴백한다.**
  D10 이 CLI·플러그인까지만 통일했던 것을 서버까지 끌고 온다. **명시가 항상 이긴다** —
  오타를 조용히 덮으면 "왜 빈 볼트인가"를 영영 못 찾는다.
- **서버판에 `--configure` 와 `/wikilens-client:setup` 을 준다.**

**대가:** 새 교차 언어 계약이 하나 늘었다 — Kotlin 이 `config.json` 의 `vault` 키를
문자열로만 알고 있다. 갈리면 예외 없이 폴백만 조용히 멈춘다. 계약이 세 파일의 키를
함께 검사한다.

**여기서 실제로 잡힌 것 둘.** 둘 다 코드를 읽어서가 아니라 **띄워봐서** 나왔다:

1. **macOS JDK 는 `HOME` 을 무시한다.** `user.home` 은 `getpwuid` 결과다(실측:
   `HOME=/tmp/x java …` → `user.home=/Users/hyunwpark`). 파이썬 `Path.home()` 은
   `HOME` 을 먼저 본다. 그대로 뒀으면 **CLI 가 쓴 설정을 서버가 다른 자리에서 찾는다** —
   서버는 systemd·cron 처럼 `HOME` 이 다르게 잡히는 환경에서 도는 물건이라 가정이
   아니라 실제 갈림이다. `HOME` 우선으로 맞췄다.
2. **볼트 경로를 푸는 곳이 둘이었다.** `IndexingService`(색인)와 `ContentService`
   (read·grep)가 각자 풀었고, 후자는 `toAbsolutePath()` 조차 안 걸어 실행 디렉터리에
   매달려 있었다. 폴백이 들어오자 갈림이 결정적이 됐다 — 실측: 폴백으로 기동한 서버가
   **문서 3건을 색인하고 검색까지 정상인데 read 는 전부 404**. 이 저장소가 이미 한 번
   겪은 실패 모양이다(조용히 실패 12번). `VaultLocator` 하나로 합쳤다.

**단순화한 것이 아니라 옮긴 것도 있다.** 자격증명 4상태 분기(`file`/`partial`/`shell`/
`none`)는 그대로다 — 그건 우리가 정할 수 있는 값이 아니라 **사용자만 입력할 수 있는
값**이기 때문이다. 단계마다 승낙을 받는 것도 남겼다(싱크는 수천 건을 요청한다).

**검증:** 빈 홈에서 setup 이 안내하는 명령을 그대로 실행해 CLI 가 **기록 없이** 잡히고
(`CLI=~/.wikilens/venv/bin/wikilens`), 래퍼가 `--root` 를 채워 `stats` 가 볼트를 읽는
것을 확인했다. 서버는 심링크도 `--wikilens.vault-root` 도 없이 폴백만으로 기동해
색인 3건 · 검색 · read 200 · grep 까지 도달했다(2026-08-07).

---

## D16. ACL 이 들어오면 학습이 균질하지 않다 — 지금 남길 것은 **범위**이지 신원이 아니다

**문제:** 서버판이 색인을 갖기로 한 이유 중 하나가 "사용자마다 랭킹 척도가 달라 학습
레이어에 이질적 관측이 섞인다"였다(D4). 서버가 **랭커**를 통일했지만, ACL 은
**후보 집합**을 다시 사용자마다 다르게 만든다. 같은 이질성이 다른 문으로 돌아온다:

- 권한이 좁은 사용자는 진짜 정답을 못 봐서 차선책을 읽고, 그 궤적이 **전 사용자의
  포스팅**을 오염시킨다.
- `rank` 가중(×1/×2/×3)이 권한 폭에 편향된다 — 목록이 짧으면 같은 문서가 낮은 순위로
  나와, 같은 발견이 사람에 따라 다른 무게로 기록된다.
- 권한이 바뀌어도 어느 궤적이 그 범위에서 나왔는지 골라낼 수 없다. 로그는 append-only
  이고 유일한 복구 불가 자산이다.

**기각한 안: `userKey` 를 궤적에 남긴다.** 위 셋을 다 풀지만 대가가 크다 — "누가 무엇을
검색했나"가 영구 기록이 된다. 이 도구는 지금 그걸 안 하고, 그건 우연이 아니라 성질이다.

**채택: 권한 토큰 집합의 해시(`AclRegistry.scopeOf`)를 남긴다.** 문제 셋이 전부
*권한 폭*의 문제이지 *신원*의 문제가 아니므로 범위만 알면 풀린다. 같은 권한을 가진
사람은 같은 값이 되고, 원문 토큰은 복원되지 않으며, 사용자 키는 해싱 전에 뺀다
(안 빼면 사람마다 달라져 결국 신원이 된다).

**지금은 기록과 집계만 한다.** 이 값으로 가중을 바꾸거나 포스팅을 쪼개는 것은 별도
설계이고 측정이 필요하다. **지금 넣는 이유는 로그 때문이다** — 나중에 넣으면 그전
궤적에는 영영 없다. `/api/stats` 의 `permissionScopes` 가 1 이면 학습이 균질하고,
2 이상이면 섞이는 중이라는 신호다(전 페이지가 `@public` 인 지금은 늘 1 이다).

**함께 고친 것:** `hints()` 가 `take(limit)` 을 권한 필터보다 **먼저** 하고 있었다.
좁은 권한의 사용자는 상위 후보가 전부 안 보이면 힌트가 통째로 0 이 된다 — 조용히
실패 8번과 같은 실패인데 그때 `SearchService` 만 고치고 여기는 남겨뒀다.

---

## D17. 궤적 로그 압축을 넣지 않는다 — 재보니 아직 문제가 아니다

**우려:** `trajectories.jsonl` 은 append-only 라 **줄지 않는다.** 포스팅은 이 로그의
함수여서 기동마다 전량 재생하므로, 로그가 커지면 기동 시간·힙·디스크가 함께 는다.
회전이나 압축이 필요한가?

**측정(2026-08-08, 합성 로그 · 페이지 2,383 · 항 6,000 · log-uniform 분포):**

| 궤적 | 파일 | 재생 | 항 | 항-페이지쌍 | 힙 |
|---|---|---|---|---|---|
| 10,000 | 2 MB | 246 ms | 4,307 | 22,393 | 3 MB |
| 50,000 | 10 MB | 345 ms | 5,939 | 89,583 | 8 MB |
| 200,000 | 42 MB | 964 ms | 5,999 | 273,496 | 24 MB |
| 1,000,000 | 210 MB | 5,261 ms | 5,999 | 901,319 | 80 MB |

재생은 궤적 수에 **선형**(약 5.3µs/건), 힙은 항-페이지쌍에 선형(약 90B/쌍)이다.

**언제 닿는가:** 궤적은 "읽기가 있는 검색" 하나당 하나다.

| 규모 | 100만 건까지 |
|---|---|
| 5명 × 10건/일 | 55년 |
| 20명 × 20건/일 | **6.8년** |
| 50명 × 30건/일 | 1.8년 |
| 200명 × 50건/일 | 0.3년 |

100만 건에서 기동 5.3초는 아무 문제가 아니다. 처음 아픈 지점은 **1,000만 건(52초)**
이고, 200명 규모로도 3년이 걸린다. 힙 상한은 항×페이지 = 14.3M 쌍 ≈ 1.2GB.

**채택: 압축을 넣지 않는다.** 이르기도 하지만 무엇보다 **위험하다** — 로그는 유일한
복구 불가 자산이고, 압축은 "어떤 학습을 버릴지" 를 정하는 일이다. 되돌릴 수 없다.

**대신 보이게 한다.** 재생 시간·로그 크기·재생 누락·쓰기 실패를 `/api/stats` 의
`trajectoryLog` 로 내고, 재생이 10초(≈200만 건)를 넘으면 기동 로그가 경고한다.
`--status` 도 같은 값을 짚는다. **압축이 필요해지는 시점을 사람이 아니라 서버가
알려주는 구조**로 바꾼 것이다.

**되돌아오는 조건:** 그 경고가 실제로 뜰 때. 그때 설계할 것은 삭제가 아니라
**체크포인트** — 포스팅 스냅샷을 쓰고 그 지점까지의 로그를 잘라내면 학습을 잃지 않고
재생이 상수 시간이 된다. 로그 자체는 (별도 보관으로) 남긴다.

**이 측정의 한계:** 합성 로그다. **시간과 바이트는 코드의 성질**이라 그대로 쓰이지만,
`항-페이지쌍`(=힙)은 질의 분포에 좌우되므로 실제 값은 다를 수 있다 — 실제 분포는 더
집중돼 있어 이 수치가 보수적일 가능성이 높다. `bench/` 를 지운 판단(D-기각 목록)과
어긋나지 않는다: 그쪽은 **검색 품질**을 합성으로 흉내 내는 것이었고, 이것은 파싱·저장
처리량이라 데이터 모양에 덜 민감하다.

---

## D18. 관리 API 는 공유 토큰으로 잠근다 — 프록시는 그 위에 얹는다

**문제:** `/api/admin/*` 에 아무 통제가 없었다. 서버에 닿는 누구나
`POST /api/admin/acl/user?userKey=자기자신` 으로 **스스로 권한을 부여**할 수 있고,
`/admin/reindex` 도 아무나 부른다. 권한을 아무리 정확히 수집해도 이게 열려 있으면
전부 무의미하다.

**셋을 놓고 골랐다:**

| | 관리 API 보호 | `userKey` 위조 | 전제 |
|---|---|---|---|
| 리버스 프록시(SSO) | ✓ | **✓** | 인프라가 있어야 한다 |
| 루프백 바인딩 | ✓ | ✗ | 서버를 공유하는 순간 성립 안 함 |
| **공유 토큰** | ✓ | ✗ | **없음** |

**채택: 공유 토큰을 바닥으로 깐다.** 리버스 프록시가 유일하게 둘 다 풀지만 **인프라를
요구하고, 없는 환경에서는 아무 보호도 못 받는다.** 공유 토큰은 전제가 없고, 프록시를
쓸 거면 그 뒤에 두면 그만이다 — 배타적이지 않다.

**기본값은 잠김이다.** 토큰을 안 주면 관리 API 가 전부 404 다. 열림을 기본으로 하면
조용히 열린 채 배포되고, 그건 이 프로젝트에서 가장 나쁜 상태다. 거부가 403 이 아니라
404 인 것은 `read` 와 같은 이유다 — 403 은 그 엔드포인트가 있다는 것을 알려준다.

**엔드포인트마다가 아니라 경로로 건다(2026-08-10).** 처음에는 핸들러가 각자
`guard.check(req)` 를 불렀다. 그러면 새 엔드포인트를 만드는 사람이 **기억해야** 하고,
잊으면 열린 채로 겉보기 정상이다 — 그리고 그 실수의 결과가 정확히 이 결정이 막으려던
"누구나 스스로 권한 부여" 다. 계약이 개수를 세고 있었지만 `@PostMapping` 만 셌다:
**인증 코드가 전혀 없는 `@GetMapping("/api/admin/dump")` 를 넣어도 계약 전부가
통과했다**(실측). 세는 것으로는 다 못 막아서 `/api/admin` 하위에 인터셉터를 걸고
컨트롤러가 가드를 아예 모르게 했다. `AclRegistry` 에 스위치를 한 곳만 둔 것과 같은
이유다.

**토큰 비교는 바이트로 한다.** 서블릿은 헤더를 ISO-8859-1 로 디코드하는데 설정값은
UTF-8 이라, 문자열끼리 비교하면 **비ASCII 토큰이 영원히 안 맞는다** — 실측으로
`admin-token=테스트토큰` 을 주고 같은 값을 헤더로 보냈는데 404 였다(ASCII 토큰은 200).
게다가 기동 경고는 값이 **비었을 때만** 나와서, 그 상태가 "토큰이 틀렸다"·"안 켰다" 와
구별되지 않았다. 지금은 동작하지만 헤더 값에 비ASCII 는 RFC 7230 밖이라 프록시가
손대면 다시 깨진다 — 기동 시 경고한다.

**이것으로 못 푸는 것:** MCP 프록시가 `config.json` 의 `user` 를 그대로 보내므로
**사용자가 자기 신원을 자기가 주장한다.** 남의 `userKey` 를 적으면 그 사람 권한으로
검색된다. 관리 API 를 잠가도 이 구멍은 그대로다 — 푸는 방법은 리버스 프록시가 헤더로
신원을 주입하는 것뿐이고, 그건 인프라 결정이라 여기서 강제하지 않는다.

---

## D19. 제한 없는 페이지는 `@public` 이 아니라 `@space:<KEY>` 다

**문제:** `wikilens acl` 이 수집할 때, 읽기 제한이 없는 페이지에 무엇을 적을 것인가.

**기각: `@public`.** 여러 스페이스를 한 볼트에 모으는 것이 정상 사용인데(이 개발
코퍼스가 12개다), `@public` 으로 적으면 **등록된 누구나 12개 스페이스를 전부 본다.**
Confluence 에서 그중 몇 개만 볼 수 있는 사람에게도 그렇다 — 과다 노출이다.

**채택: `@space:<KEY>`.** 페이지가 속한 스페이스가 **계산 가능한 상한**이다. 누가 어느
스페이스를 볼 수 있는지는 운영자가 `acl/user` 로 정한다.

**스페이스 권한 API 는 안 쓴다.** Server/DC 는 버전마다 다르고, 여기서 필요한 것은
"이 페이지가 어느 스페이스인가" 라는 상한뿐이다. 그 이상은 운영자의 몫으로 남긴다.

**함께 결정한 둘:**

- **상속을 직접 푼다.** `byOperation` 은 그 페이지에 **직접** 걸린 제한만 준다.
  Confluence 는 가장 가까운 조상에서 상속하므로, 직접 제한만 보고 "없음 = 공개" 로
  적으면 **상속으로 잠긴 문서가 통째로 노출된다.** `ancestors` 는 싱크가 이미
  저장해 두므로 추가 요청 없이 풀린다.
- **조회 실패는 공개로 바뀌지 않는다.** 실패를 "제한 없음" 으로 뭉개면 네트워크 오류
  한 번이 노출이 된다. 실패한 페이지는 **이전 파일의 값을 그대로 옮겨 담고**, 처음 보는
  페이지는 아예 빼서 아무에게도 안 보이게 한다(fail-closed).

**이 fail-closed 를 완성하는 데 세 군데가 더 필요했다(2026-08-10).** 술어는 맞았는데
적용되는 자리가 빠져 있었고, 셋 다 조용했다.

1. **조상의 실패는 안 막고 있었다.** 페이지 자신의 실패만 막고, 상속을 푸는 쪽은 못
   읽은 조상을 "제한 없음" 과 똑같이 취급하고 계속 위로 올라갔다 — 실측: 부모가 500 을
   내자 자식이 `@space:SEC` 를 받았다. 이 코퍼스의 조상 깊이 중앙값이 5라 실패 한 건이
   그 아래 가지 전체에 걸린다. 이제 못 읽은 조상에서 멈추고 `unresolved` 로 센다.
   싱크 집합 밖의 조상도 조회한다 — 안 그러면 "안 가져왔다" 와 "제한 없다" 가
   구별되지 않는다(실측: 13,921건에서 그런 조상은 2개뿐이라 비용이 없다시피 하다).
2. **서버가 그 생략을 되돌리고 있었다.** `VaultReader` 가 `acl.json` 에 없는 페이지를
   `@public` 으로 채웠다 — 파일로만 이어진 두 판이 각자 합리적인 기본값을 고르다
   정반대가 된 자리다. "수집한 적 없음"(= ACL 이전 볼트, 전부 공개가 맞다)과
   "수집했는데 이 페이지가 없음"(= 확정 실패)을 나눴다. 파일이 있는데 파싱에 실패하면
   전자가 아니라 후자다 — 아니면 깨진 파일 하나가 전 페이지를 공개로 만든다.
3. **전부 실패한 결과를 덮어썼다.** `acl` 은 페이지마다 낱개 조회를 해서 이 프로젝트
   에서 API 를 가장 세게 쓰는데(13,921건이면 요청도 그만큼), 429 백오프가 `_paged`
   안에만 있어 그 경로는 보호를 못 받았다. 429 가 쏟아지면 전부 "실패" 가 되고 결과
   `{}` 를 파일에 쓴다 — 2번을 고친 뒤로 그것은 **전 페이지 블랙아웃**이다. 백오프를
   `_get` 으로 옮기고, 전부 실패하면 파일을 쓰지 않는다. 볼트를 못 읽은 재기동이
   멀쩡한 색인을 지우던 것과 같은 규칙이다 — 못 읽은 것과 없는 것은 다르다.

---

## D20. ripgrep 엔진을 붙인다 — 두 경로는 **대조로** 다룬다

> **2026-08-10 정정:** 아래 "2.44초 / 약 17,000건" 은 **재현되지 않는다.** 같은 코퍼스를
> 같은 하네스로 다시 재니 1.21초(문서당 86us)였고, 합성 볼트에서 유도한 모델
> (문서당 ≈ 28us + 13us/KB)이 그 코퍼스의 평균 문서 크기 3.9KB 로 예측한 80us 와
> 8% 안에서 맞는다. 즉 **한계는 약 17,000이 아니라 35,000건대**였고, rg 를 붙일
> 긴급성이 2배 과장돼 있었다. 결정 자체는 유지한다(rg 가 여전히 문서당 3.3배 싸고
> 경로 대조 장치도 그대로 값어치가 있다) — 다만 **근거 수치는 이 정정을 통해 읽을 것.**
> 다시 재는 장치는 `GrepScaleTest` 다. 아래는 당시 서술이다.

**문제:** JVM 스캔이 13,921건에 2.44초인데 예산이 3초다. 약 17,000건에서 상시 잘림이
되고, 그건 실패가 아니라 **조용한 부분 응답**이라 사용자에게 "없다"로 보인다.

**대안이 둘이었다.**

| | 이득 | 대가 |
|---|---|---|
| 문서 루프 병렬화 | 코어 수만큼 (예상 5~8배) | **없음 — 경로가 하나** |
| ripgrep 프로세스 | D12 주장 65배 | 경로 둘 · 프로세스 기동 · rg 부재 시 폴백 |

rg 를 골랐다. **다만 실측하니 65배가 아니라 4~6배였다** — 병렬화 예상치와 같은
자릿수다. 미리 알았다면 병렬화가 나은 선택이었을 수 있다.

**실측(2026-08-08, 13,921건, 3회 중앙값):**

| 패턴 | JVM | ripgrep | 배수 |
|---|---|---|---|
| `정책`(흔한 낱말) | 0.32s | 0.06s | 5.3× |
| `DEPLOY_TOKEN` | 1.17s | 0.30s | 3.9× |
| `zzqqxxnomatch`(전량 스캔) | 1.18s | 0.20s | 5.8× |
| `배포\|릴리스`(정규식) | 0.21s | 0.06s | 3.7× |
| `[가-힣]{4,}`(즉시 limit 도달) | 0.02s | 0.04s | **0.4×** |

마지막 줄이 이유를 보여준다 — 스캔이 거의 없으면 rg 는 **프로세스 기동 때문에 더
느리다.** 그리고 ACL 을 걸어 대상 목록을 만드는 고정비는 두 엔진이 똑같이 낸다.
예산 한계는 17,000 → 약 70,000~85,000건으로 밀렸다.

**경로가 둘인 것은 피할 수 없다.** rg 가 없는 머신이 있어 폴백이 영구히 공존한다
(이 개발 머신에도 없어서 `brew install ripgrep` 을 했다 — 그전까지 `rg` 는 Claude Code
가 넣어둔 **셸 함수**였고 `ProcessBuilder` 는 그걸 못 쓴다). 남은 선택은 "갈리는지
모르는 채로 두기" 와 "갈리면 빨개지게 하기" 뿐이라 후자를 만들었다:

  - **`GrepEngineParityTest`** — 같은 볼트에 14개 패턴(대소문자·한글·정규식·특수문자)을
    돌려 두 엔진의 매치를 대조한다. `-i` 를 빼거나 ACL 필터를 지우면 실제로 빨개지는
    것을 확인했다. rg 가 없는 머신에서는 건너뛴다.
  - **ACL 은 엔진 밖에 있다.** `ContentService` 가 거른 목록만 넘긴다 — 권한 해석이
    엔진마다 갈리면 한쪽이 조용히 더 보여준다. 계약이 엔진의 `AclRegistry` 참조를 막는다.
  - **응답에 `engine` 을 싣는다.** `auto` 는 머신에 rg 가 있는지에 따라 갈리므로,
    답이 이상할 때 어느 경로였는지 물을 수 있어야 한다.
  - **`--no-config`·`--no-ignore` 가 필수다.** 없으면 운영자의 `~/.ripgreprc` 나 볼트에
    생긴 `.gitignore` 가 **같은 질의를 머신마다 다른 답**으로 만든다.

**파일 목록을 인자로 넘기지 않는다.** ACL 통과분만 주는 편이 정확하지만 13,921개면
`ARG_MAX` 를 넘는다. `mirror/pages` 를 통째로 주고 **결과를 받아서 거른다** — rg 가 못 볼
문서까지 읽는 낭비가 있지만 유출은 아니고, 4~6배가 그 낭비를 덮는다.

**예산은 감시 스레드가 건다(2026-08-10).** 처음에는 읽기 루프 안에서 시계를 봤는데,
**rg 는 매치가 있을 때만 줄을 낸다.** 매치 없는 스캔은 EOF 까지 블록되고 그동안 마감
검사가 한 번도 안 돈다 — 실측: 예산 1ms 를 주고 13,921건을 훑는 데 **738ms**(JVM
경로는 79ms 에 끊었다). 즉 예산 3초는 JVM 경로에만 걸려 있었고, rg 경로의 실제 상한은
"rg 가 끝날 때까지" 였다. rg 자체에 상한이 없으니 큰 코퍼스나 느린 볼트에서는 요청
스레드가 그만큼 잡히고, stderr 가 차서 막히는 경우도 같은 구멍이었다.

감시 스레드가 마감에 프로세스를 죽이면 stdout 이 풀린다. EOF 가 아니라 `IOException`
으로 끝나므로 그것을 **오류가 아니라 우리가 시킨 마감**으로 다뤄 받은 매치를 살린다.
고친 뒤 34ms.

**완주와 마감을 구별한다.** 예전에는 남은 예산으로 프로세스를 거두고 그것이 만료되면
잘렸다고 했는데, 그건 거두는 데 걸린 시간이지 스캔이 잘린 것이 아니다 — 위 실측에서
전량을 훑고도 `truncated=true · scanned=0` 이 나왔다. 매치 0건이 확정인데 "잘렸다" 로
보였다. 거두기를 예산 밖 고정 여유로 옮겨서, 이제 `truncated` 는 "우리가 정말 끊었다"
는 뜻이다. `RipgrepBudgetTest` 가 이것을 잰다 — 실제 코퍼스가 없으면 건너뛴다.

---

---
## 기각된 소소한 것들

- **RRF에 신뢰도 부스트** (`RRF × (1+α·rel)`) — 합성 시뮬레이션에서 **3.3% 더 나빴다.**
  신뢰도를 정답 확률과 일치시켜 최대한 유리하게 만들었는데도 졌다. 억지로 튜닝해
  살리지 않았다. RRF 유지. (근거였던 `bench/`는 2026-08-04 제거돼 재현 불가 —
  게다가 그 시뮬레이션의 RRF는 순위만 쓰는 형태라 지금 배선된 신뢰도 가중 융합과
  다르다. 이 결론을 다시 근거로 삼으려면 실측부터 해야 한다.)
- **고정 IDF 임계** — BM25 IDF는 소규모 코퍼스에서 압축된다(문서 4개면 df=1 항의
  IDF가 1.20, 10개면 1.99). 코퍼스 크기에 연동해야 한다.
- **싱크 레이어 자체 구현** — `confluence-markdown-exporter`가 있지만,
  원본 XHTML 보존과 구조화된 앵커 추출이 필요해 결국 직접 구현했다.
  다만 **Kotlin으로 재구현하는 것은 기각** — Python 것이 이미 검증돼 있다.
- **Go** — 서버가 카운터만 들던 시점엔 유효했으나, 색인이 돌아오면서 한국어 형태소
  분석(Lucene Nori)이 필수가 됐다. Go의 유일한 이점인 단일 바이너리가
  cgo mecab 때문에 사라진다.

---

## 정체 규정

**RAG가 아니다.** 반환값이 내용이 아니라 좌표이고, 검색이 생성이 아니라 행동 선택을
조건화하며, 인덱스가 시스템 자기 동작의 산출물이다. RAG로 부르면 벡터 DB·청킹·
검색 정밀도 지표라는 반사가 따라오는데 이 도메인에서는 전부 어긋난다.

**LTR도 아니다.** 특징 벡터도 모델도 손실 함수도 없다. LTR의 **입력(특징)을 만드는
단계**이고, 지금 랭커는 RRF(비지도)다. 궤적이 수십만 건 쌓이고 특징 파이프라인이
생기면 그때가 LTR로 졸업할 시점이다. 다만 LTR은 `n`을 줄이므로 **L2와 대체 관계**다.

가까운 계보는 **에이전트 절차 기억** — AWM(ICML'25), APC(NeurIPS'25).
엄밀히는 절차보다 **일화 기억**에 가깝다.
