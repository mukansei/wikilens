# 결정 로그

이 프로젝트는 긴 설계 논의에서 나왔고, **여섯 번 크게 뒤집혔다.**
아래는 "이미 시도했다가 기각된 것"의 목록이다. 되돌리기 전에 읽을 것.

항목이 세 종류다:

- **D1~D6, D8~D11** — `기각된 안 → 채택된 안 → 뒤집힌 이유`. 되돌리기 전에 읽을 것.
- **D7** — 뒤집힌 결정이 아니라 **지우지 말아야 할 것**. 지금 놀고 있지만 되살아날
  조건이 분명해서 "안 쓰이니 지우자"는 판단이 반복될 자리다.
- **D12** — 결정이 아니라 **측정 기록**. 코퍼스가 커질 때 무엇이 먼저 깨지는지를
  미리 재둔 것이라, 그 규모에 닿기 전에 열어볼 자리다.

---

## D1. 경로 압축 → 폐기 (조건부 보류)

**기각:** Union-Find식 경로 압축으로 탐색 그래프를 최적화.

**이유의 변천:**
1. 처음엔 "의미적 유사성은 추이적이지 않다"로 기각했다.
   정규화 벡터에서 `cos(A,B)=cos(B,C)=0.8`이면 `cos(A,C)`의 하한이
   `2(0.8²)−1 = 0.28`이고 이 하한은 실제로 달성 가능하다(계산 확인).
2. **그 논거는 나중에 무효가 됐다.** 간선이 "유사하다"가 아니라 "이 경로가
   유용했다"만 주장하고, 읽기 전용이라 사람이 그 간선을 읽지 않는다.
3. **진짜 이유(2026-08-04 재검토로 교체):** **압축할 체인이 애초에 존재하지 않는다.**
   `postings` 는 `항 → 페이지 → (hits, misses)` 인 **이분 구조**지 페이지→페이지
   그래프가 아니다. Union-Find 경로 압축은 `parent[node] → node` 포인터 숲이 있어야
   성립하는데 그런 구조가 없다. 게다가 간선을 만드는 `apply()` 는 `dest`(= `reads.last()`)
   하나만 기록하므로, 세션이 A→B→C 를 읽어도 남는 것은 `항 → C` 하나다 —
   **기록 시점에 이미 최대로 압축돼 있다.**

   > 이 자리에는 원래 "앵커가 홉 수를 6.04→1.81로 줄여 압축할 게 안 남았다"가 적혀
   > 있었다. 그 수치는 무관한 두 실험 값을 이어붙인 오기였고(실제 6.04→3.33),
   > 더 근본적으로는 **범주 오류**였다: `n` 은 랭킹 리스트 스캔 깊이지 그래프 경로
   > 길이가 아니고, 그 `n−1` 을 아껴주는 것도 경로 압축이 아니라 L2 숏컷 자체다.
   > 정정된 값은 오히려 학습 레이어에 유리하다 — 손익분기 `p_wrong/(n−1)` 의 문턱이
   > n=1.81 대비 약 2.9배 낮아진다. 근거였던 합성 시뮬레이션(`bench/`)은 이후 제거됐다.

**되돌아오는 조건:** `n` 이 커지는 것과는 무관하다(그때도 압축할 그래프는 없다).
**중간 읽기를 별도 간선으로 기록하기 시작하면** 비로소 체인이 생겨 압축이 의미를 갖는다 —
다만 그것은 "압축이 아니라 가산" 원칙이 이미 금지하는 방향이다.

**부수 논점:** "압축 대 가산"은 알고리즘 차이가 아니라 구현 선택이다.
가법 Union-Find도 같은 상수 시간 이득을 준다. 다만 여기서는 가법이 **강제**인데,
목적지가 시작 노드만의 함수가 아니라 질의에 따라 달라지기 때문이다.

---

## D2. 손익분기 `1/n` → `p_hit > p_wrong/(n−1)`

**기각:** `E[L2] = p·1 + (1−p)(1+n)` → 손익분기 `p > 1/n`.

**오류:** 케이스를 둘로 뭉갰다. 실제로는 셋이고, **"숏컷이 없음"은 헛읽기 비용이
0인데** 그것을 실패에 포함시켰다.

```
적중        1 읽기      p_hit
잘못된 숏컷  1+n 읽기    p_wrong
숏컷 없음    n 읽기      p_none     ← 손해 없음

p_hit·1 + p_wrong(1+n) + p_none·n < n   ⟹   p_hit(n−1) > p_wrong
```

**함의:** 기준이 절대 적중률이 아니라 **적중 대 오답 비율**이다. 캐시가 확신 없을 때
침묵하면 `p_wrong → 0`이라 거의 항상 이득이다. EB 게이트가 하는 일이 그것이다.

**발견 경위:** 0% 워밍업에서 `E[L2] < n`이라는 물리적으로 불가능한 값이 나와 추적했다.
별개로 베이스라인 `n`을 고유 키 평균으로 계산한 버그도 있었다(Zipf 반복 무시).

---

## D3. Wilson 하한 → Empirical Bayes

**기각:** 신뢰도를 Wilson score 하한으로만 계산.

**이유:** Wilson은 카운트만 본다. 같은 4승 3패라도 검색이 강하게 가리키는 후보와
겨우 걸린 후보를 구분하지 못한다. **Wilson은 균등 사전(정보 없음)의 특수한 경우**이므로,
검색 점수를 사전분포로 주면 그 정보가 살아난다.

```
prior = Beta(s·κ, (1−s)·κ)      s = 정규화된 검색 점수
post  = Beta(s·κ + hits, (1−s)·κ + misses)
```

사전분포는 데이터가 없을 때만 일한다 — 400승 300패에서 격차가 0.005로 사라진다.
그래서 탐색을 위해 서빙 품질을 희생할 필요가 없다.

**함께 기각:** ε-randomization. 탐색을 위해 서빙을 희생하는 건 손실이다.
그리고 애초에 편향이 아니라 **표본 부족**이었다 — 적중률 자체는 순위와 무관하게
같았고, 웹 검색과 달리 여기서는 검토 여부가 관측 가능하므로 IPS도 불필요하다.

---

## D4. 서버 색인 → 클라이언트 분산 → 서버 색인 (두 번 뒤집힘)

**최종: 서버가 전체 코퍼스를 색인한다.**

클라이언트 분산 색인을 잠시 채택했던 이유는 ACL 회피였다. 기각한 이유:

| | 클라이언트 분산 | 서버 색인 |
|---|---|---|
| Confluence API 부하 | 사용자 수 비례 (**200명이면 200배**) | 1배 |
| dense 임베딩 | 중복 계산 | 1회 |
| IDF | 권한 좁은 사용자는 추정 붕괴 | 전역 안정 |
| **랭킹 척도** | **사용자마다 다름** | 동일 |
| 보고서(고아·누락 링크) | 개인 뷰라 무의미 | 전역 그래프 |

넷째 줄이 결정적이다. 척도가 다르면 EB 사전분포로 들어오는 점수가 서로 달라
**이질적인 랭커의 관측을 하나의 카운터에 섞게** 된다.

"서버측 코퍼스가 ACL 문제의 원천 전부"라고 했던 건 과장이었다. 카운터만 둬도
질의 키워드는 서버로 가고, 질의 시점 ACL은 사내 검색의 표준이다.

---

## D5. 볼트 배포 → 서버 서빙

**기각:** 서버가 ACL 필터링한 볼트를 클라이언트에 배포.

**이유:** **배포된 사본은 회수할 수 없다.** 사용자가 팀에서 나가도 노트북의 볼트에는
전체 사본이 남는다. 오프라인이면 삭제 명령이 도달조차 못 한다.
권한 시행에 그토록 공을 들였는데 배포가 그것을 무효화한다.

**연쇄 단순화:** 서버가 읽기를 서빙하면 **훅이 통째로 불필요해진다** — 서버가 궤적을
직접 관측하므로 클라이언트 버퍼링·핫패스 비용·세션 조립이 사라진다.
플러그인이 `스킬+훅`에서 **MCP 도구 4개**(`search`/`read`/`grep`/`tree`)로 바뀐다.

**함께 정정:** "서버판 = 로컬판 + 서버"가 아니다. 코퍼스 위치·인터페이스·ACL 방식이
전부 다르므로 **증분이 아니라 전환**이다.

---

## D6. 궤적 보고 → 관측

**기각:** 에이전트가 `record_trajectory` 도구를 호출해 궤적을 보고.

**이유:** 에이전트는 "작업 보고" 성격의 도구를 안정적으로 호출하지 않는다.
L2 전체가 그 호출에 의존하면 데이터가 안 쌓인다.

**중간 안(로컬 볼트 + 훅):** Claude Code 훅이 네이티브 `Read`를 관측할 수 있다.
페이로드에 `session_id`가 있어 궤적 조립이 된다. **이것도 D5로 폐기됐다** —
서버가 읽기를 서빙하면 훅 없이 직접 관측된다.

**교훈:** "가능하다"와 "그래야 한다"는 다르다. 훅은 로컬 볼트에 대한 *반대 논거를
제거*했을 뿐인데 한동안 *찬성 논거*로 읽었다.

---

## D7. `mirror/structure/` 유지 (지금은 아무도 안 읽는데도)

**현황:** `build`가 페이지마다 구조 서명(제목·헤딩·링크)을 `mirror/structure/` 에 쓰지만
**읽는 코드가 한 곳도 없다.** Kotlin 서버는 `structure` 라는 단어조차 없다.

**만든 이유:** 무효화 대상을 좁히려는 것이었다. 산문만 고친 수정은 `pages/` 만 바꾸고
이 파일은 그대로여야 하므로, `git diff -- structure/` 한 줄이 "구조가 실제로 바뀐
페이지" 목록이 된다. `canonical_json` 의 결정적 직렬화와 `to_dict()` 의 링크 정렬이
전부 이걸 위해 존재한다 — diff 가능한 스냅샷이어야 하기 때문이다.

**왜 놀고 있나:** 무효화 메커니즘 자체가 구현된 적이 없고, 더 근본적으로 "증분 색인을
안 한다"는 결정이 전제를 없앴다. 전체 재구축이 수 초면 무엇이 바뀌었는지 알 필요가 없다.

**그런데 지우지 않는다.** 코퍼스가 커지면 되살아나는데, 그 이유가 처음 의도와 다르다:

- **변경 감지 용도로는 이미 대체됐다.** 어떤 페이지가 바뀌었는지는 `.sync-state.json` 의
  `version` 이 알려주고 `sync` 가 그걸로 증분 페치를 한다. 더 싸고 이미 있다.
  구조가 바뀌었는지 알려면 파싱해야 하므로, 파싱을 피하려고 structure 를 보는 건 순환이다.
- **남는 역할은 과잉 무효화 방지다.** `version` 은 너무 거칠다 — 실측(Coway 2,378건)에서
  80%가 한 번 이상 수정됐고 중앙값 v4, 최대 v145다. 오탈자 하나에도 버전이 오르므로
  version 기준 무효화는 학습 간선을 거의 전량 날린다. `structure/` 는 "링크·헤딩이
  실제로 바뀐 경우"만 골라낸다.

**되돌아오는 조건:** 증분 build 를 도입할 때. 실측(2026-08-06)하면 전량 build 가
2,377건에 29.9초라 10만이면 **21분**이다(D12) — 그 규모에서는 전량 재파싱이
성립하지 않고, 증분이 들어오는 순간 "어떤 무효화가 정당한가"가 곧바로 문제가 된다.
**증분 build 와 `structure/` 는 한 세트다.**

**유지 비용이 거의 없다는 것도 근거다.** 파일당 평균 362B(10만이면 40MB, 100만이면
0.4GB), 2,378개 읽기가 59ms. `_write_if_changed` 덕에 실제 쓰기는 변경분만 일어난다.
지금 지웠다가 나중에 되살리면 diff 의 기준점이 될 이력이 없어 처음부터 다시 쌓아야 한다.

> 단, `headings` 는 추출만 하고 아무도 안 쓴다. 링크는 `anchors.jsonl` 을 뒤집으면
> 복원되므로(미해결 링크 72건 제외) `structure/` 고유 정보는 실질적으로 그 둘뿐이다.
> 증분 build 를 실제로 만들 때 이 스키마가 그대로 필요한지 재검토할 것.

---

## D8. 로컬판을 배포 가능하게 (볼트 고정 위치 + 스킬 절대경로화)

**문제:** 로컬판은 **구조적으로 배포가 불가능**했다. 스킬이 전부 cwd 상대경로
(`Grep(path="ALIASES.md")`)라 **작업 디렉터리가 볼트일 때만** 동작하는데, 볼트는
어느 프로젝트도 아닌 곳에 있다. 전역 설치하면 볼트 아닌 프로젝트에서 발동해 없는
파일을 grep한다 — description 이 공격적이라 실제로 발동한다.

**채택:** 볼트 경로를 `~/.wikilens/config.json` 에 기록하고, 스킬이 매번
`scripts/vault_status.py` 로 경로와 상태를 받아 모든 Grep/Read 앞에 붙인다.
설정은 `/wikilens-local:setup` 커맨드가 안내한다.

**볼트 배포 금지 계약과 충돌하지 않는다.** 배포하는 것은 스킬과 스크립트뿐이고
볼트는 **각자 자기 토큰으로 만든다**. 오히려 ACL 이 설계상 해결된다 — 볼 수 없는
문서는 애초에 볼트에 안 들어온다(서버판이 지금 막혀 있는 그 문제). 대가는 Confluence
부하가 사용자 수에 비례하는 것으로, D4 에서 서버 색인을 택한 이유와 같은 트레이드오프다.
**로컬판 배포는 ACL 을 부하와 맞바꾸는 선택**이라, 소규모 팀에는 유효하고 대규모에는
서버판이 맞다. 상한이 실측돼 있다 — 10만 문서면 `ALIASES.md` 흔한 말 매치가
5,687줄이 되어 컨텍스트가 범람한다(D12). 부하가 아니라 **여기서 먼저 못 쓰게 된다.**

**기각한 대안:**

- **환경변수만으로 볼트 지정** — Claude Code 가 띄우는 셸에서 `export` 해도 다음
  세션엔 없다. "어디서든 검색"이 세션마다 깨진다. `WIKILENS_VAULT` 는 일회성 재정의로만 남김.
- **setup 을 스킬로** — 스킬은 description 매칭으로 발동해서 "볼트가 없을 때만"을
  표현할 수단이 없고, 검색 스킬과 같은 발화에서 경쟁한다. 사용자가 명시적으로 부르는
  동작이므로 커맨드가 맞다.
- **CLI 를 플러그인에 동봉** — 설치는 **버전별 캐시로 복사**되고 구버전은 청소되므로
  `pip install -e ${CLAUDE_PLUGIN_ROOT}/cli` 는 캐시가 지워질 때 죽는다. 게다가
  `marketplace/plugins/` 수동 사본 6개를 없앤 것과 같은 실수를 CLI 에서 반복하는 셈이다.
  대신 `known_marketplaces.json` 의 `installLocation` 에서 CLI 소스를 찾는다.
- **로컬판도 MCP 로 통일** — 로컬판의 정의적 성질이 "검색 경로 런타임 의존성 0"이다.
  MCP 를 붙이면 첫 검색 전에 프로세스 기동이 필요해지고 파이썬 환경 문제가 검색 실패로
  나타난다. 두 판이 **다른 배포 형태**라는 D5 의 구분도 흐려진다.
  다만 MCP 는 권한 프롬프트와 경로 해석을 공짜로 푼다 — **실사용에서 권한 마찰이
  못 견딜 수준이면 그때 전환한다.** 전환 비용은 낮다(서버판 프록시와 그 테스트
  하네스가 이미 있다).
- **볼트를 플러그인에 넣어 배포** — 계약 위반이고, 관리자 권한 범위의 코퍼스가
  통째로 유출된다. 크기(59MB)는 부차적이다.
- **필요할 때만 조금씩 받기** — 원리적으로 불가. `build.py` 가 3패스라 전체 제목→ID
  색인이 있어야 링크가 해석되고, 앵커 전치는 모든 문서의 링크를 봐야 한다.

**실측으로 확인한 것:** 플러그인 설치는 소스 편집을 **자동 반영하지 않는다**(캐시 복사).
볼트 읽기는 프로젝트마다 승인이 필요하다(이 저장소 `settings.local.json` 에
`Read(//Users/…/wiki/**)` 흔적). 두 판의 SKILL.md 가 `name: wikilens` 로 같고
description 이 거의 동일해 동시 설치 시 오선택 위험이 있었다 — 양쪽 description 을
배타적으로 고쳤고 `shared_contract.sh` 가 동일해지는 것을 막는다.

**아직 검증 못 한 것:** 이 저장소에 git remote 가 없어 `pip install git+URL` 분기,
타 머신 첫 설치, git source 마켓플레이스는 미검증이다. 다만 같은 머신에서
`coway-dt` 가 `https://github.com/cw-smart-catalogue/dt-monitoring.git` 에서 설치돼
동작 중이므로 git source 경로 자체는 실재한다 — wikilens 로 확인만 안 됐을 뿐이다.

---

## D9. 플러그인 이름은 불변, 바꾸려면 `renames` 에 적는다

**문제:** 2026-08-05 에 플러그인 이름을 바꿨다. 서버판이 무표지 `wikilens` 를 차지해
"기본"을 참칭했는데 실제 배포 가능한 것은 로컬판이라, `wikilens-client` /
`wikilens-local` 로 갈랐다(디렉터리도 `plugin/client/` — 저장소의 `server/` 가 진짜
Kotlin 서버라 같은 단어가 두 물건을 가리켰다). 마켓플레이스도 `wikilens-tools` →
`wikilens`.

**이번엔 공짜였다. 다음엔 아니다.** 공식 마켓플레이스 README 가 명시한다 — 플러그인
`name` 은 **불변 슬러그**이고, 게시 후 바꾸면 사용자 설치가 `plugin-not-found` 로
깨진다. 아직 아무 데도 게시하지 않아 아무 일도 없었지만, 사내 git 에 올리는 순간부터
같은 제약이 걸린다.

**채택:** 매니페스트 최상위에 `renames` 맵을 둔다. 로더가 이걸 읽어 옛 슬러그를 새
슬러그로 다시 써서 기존 설치를 자동 이전시킨다.

```json
"renames": { "wikilens": "wikilens-client" }
```

지금 이 한 줄은 **기능적으로 불필요하다** — 구 이름으로 설치한 사람이 없다.
남겨두는 이유는 다음에 이름을 바꿀 사람에게 여기 적으라고 알리는 것이다.
`shared_contract.sh` 가 대상이 실재하는지, 살아 있는 이름을 키로 쓰지 않았는지 본다
(둘 다 틀리면 조용히 아무 일도 안 한다 — 이전이 안 되거나 플러그인이 자기 자신에서
밀려난다).

**공식 디렉터리 등재는 지금 대상이 아니다.** PR 이 아니라 심사 제출이고
(`clau.de/plugin-directory-submission`), 문서가 전부 한국어에 사내 게이트웨이 우회가
절차에 박혀 있으며, 무엇보다 **서버판이 ACL 수집 전이라 다중 사용자에 안전하지 않다.**
팀 배포 목적이면 사내 git 마켓플레이스가 맞다.

---

## D10. 설정 정본을 `~/.wikilens/` 로 (양판 공통)

**문제:** 자격증명과 서버 주소가 **환경변수 전용**이었다. `export` 는 그 셸에서만
산다. 결과가 판마다 다르게 나타났다:

- 로컬판 — 검색은 되는데 `sync` 만 죽는다. 사용자는 정상인 줄 알고 갱신을 자기
  터미널에서 수동으로 하고 있었다. `/wikilens-local:sync` 는 **한 번도 동작한 적이 없었다.**
- 서버판 — Claude Code 를 앱으로 띄우면 환경이 비어 서버 주소가 조용히 localhost
  기본값이 되고 식별자가 없어 **모든 검색이 빈 결과**가 된다.

볼트 경로는 같은 이유로 이미 `config.json` 으로 옮겨져 있었다(D8). **자격증명만
그 교훈에서 빠져 있었다.**

**채택:** 정본은 `~/.wikilens/` 하나이고, 안에서 둘로 나눈다.

| | 무엇 | 왜 |
|---|---|---|
| `config.json` | 볼트 경로 · CLI 경로 · 서버 주소 · 사용자 식별자 | 비밀이 아니다 |
| `env.sh` (600) | Confluence 토큰류 | 비밀이다. **JSON 이 아니라 셸 스크립트** — 사용자가 자기 터미널에서 `source ~/.wikilens/env.sh` 로 그대로 재사용한다 |

환경변수는 남기되 **일회성 재정의**로만 이긴다.

**CLI 경로도 여기 기록한다.** venv·pipx 에 설치하면 그 셸을 활성화하지 않는 한 PATH
에도 없고 기본 python 으로 import 도 안 되는데, Claude Code 가 띄우는 셸이 정확히
그 상태다(실측: `cli/.venv` 에 설치했더니 래퍼가 못 찾았다). 해석처는
`vault_status.py` 하나이고 래퍼는 `--cli` 로 물어본다 — 둘이 각자 찾으면 스킬은
"CLI 있음"이라 하는데 래퍼는 못 찾는 상태가 생긴다.

**기각한 대안:** macOS 키체인(첫 조회 시 GUI 승인 팝업이 백그라운드 셸을 멈출 수
있고 macOS 전용), `config.json` 에 토큰까지(셸에서 재사용하려면 파싱이 필요해
수동 싱크 경로가 불편해진다).

**검증:** PATH 를 `/usr/bin:/bin` 으로 좁힌 셸에서 래퍼가 CLI 에 도달하고 실제
Confluence 인증까지 통과하는 것을 확인했다(2026-08-05). 로컬판 싱크 경로가 Claude
Code 안에서 처음으로 끝까지 동작한 시점이다.

---

## D11. 샤딩은 페이지 ID 의 **뒤** 2자리, 1단계

**문제:** 앞 4자리를 2단계로 쪼개고 있었다. 근거는 "10k 페이지면 리프당 1개 미만"
이었는데 **균등 분포를 가정한 계산**이었다. 실제로는 10,000개 리프 중 52개만 쓰고
한 디렉터리에 최대 378개가 몰렸다.

**측정:** Coway 2,377건 (2026-08-06)

| | 디렉터리 | 리프 최대 | 평균 |
|---|---|---|---|
| 앞2/앞4 (구) | 52 | 378 | 45.7 |
| 뒤2/뒤4 | 2,170 | 3 | 1.1 |
| **뒤2 (채택)** | **100** | **37** | **23.8** |

구 규칙은 2단계를 쓰고도 466→378, 18% 줄었을 뿐이다. 뒤에서 쪼개면 1단계로 열 배 낫다.
`뒤2/뒤4` 는 파일 2,377개에 디렉터리 2,170개라 과하다 — 그래서 depth 를 1로 줄였다.

**"몇 개부터 문제인가"는 재보니 거의 무관했다.** 구 주석의 "수천 개"에는 근거가
없었다. 실측(APFS): 한 디렉터리에 100개일 때 직접 열기 1000회가 23.5ms, **50,000개일
때 37.8ms** 다. 500배를 넣어도 개당 0.024→0.038ms 이고, 코드는 `ALIASES.md` 에 적힌
경로로 **직접 열지 나열하지 않는다**. 선형으로 느려지는 건 나열뿐이다(0.1ms → 25.3ms).

그래서 샤딩의 진짜 수혜자는 이 코드가 아니라 **나열하는 쪽**이다 — git·Finder·rsync·
편집기(Obsidian 으로 볼트를 여는 사용이 실제로 있다), 그리고 APFS 만큼 관대하지 않은
파일시스템. 비용이 0 이라 보험으로 유지하지만, **문서량 때문에 depth 를 올릴 일은
사실상 없다**(100만 문서 → 디렉터리당 1만 개, 위 실측 범위 안).

코퍼스가 커질 때 실제로 먼저 깨지는 것은 따로 있다 — **D12** 에 측정해 뒀다.

**왜 앞자리에 엔트로피가 없나 — 처음엔 추론이었다가 확인했다.**

"시간순 연속 할당"이라고 단정해 커밋했는데 근거를 요구받고 실제로 쟀다:

- 자릿수별 엔트로피 1번째 1.93 bit vs 9번째 3.32 bit (10값 균등이 3.32)
- 인접 ID 간격 중앙값 13, 46%가 10 이하. 폭 3.3억에 2,377개가 균등이었다면
  중앙값이 약 138,000 — 네 자릿수 차이다
- REST API 로 표본 31개의 `history.createdDate` 를 받아 대조: **스피어만 ρ=0.9964**,
  역순 465쌍 중 6쌍(1.3%), 2022-09~2026-08

메커니즘은 **Hi/Lo** 다. Confluence 는 `hibernate_unique_key` 의 `next_hi` 로 ID 를
만든다 — DB 에서 high 를 받아 메모리에서 low 를 채우고 소진되면 다시 받는다. 관측이
그대로 설명된다: low 안은 연속(간격 13), high 가 건너뛰며 큰 구멍(폭 3.3억), high 가
느리니 앞자리 엔트로피가 낮고 low 가 블록을 채우니 뒷자리가 최대다. Confluence 공통
구현이므로 다른 인스턴스도 같은 분포를 기대할 수 있다.

**대가:** 온디스크 레이아웃을 깨는 변경이다. 이 머신에만 볼트가 있어 지금은 공짜였고
(7,131개 파일을 md5 대조하며 이전), 사내 git 에 올린 뒤라면 이전 경로를 제공해야 한다.
규칙이 Python·Kotlin·플러그인 3곳에 복제돼 있어 셋을 함께 옮겼다.

---

## D12. 10만 문서에서 먼저 깨지는 것 (샤딩이 아니다)

**결정이 아니라 측정 기록이다.** "코퍼스가 커지면 샤딩 depth 를 올린다"는 계획이
D11 실측으로 근거를 잃었으므로, 그 자리에 **실제로 먼저 깨지는 것**을 적어둔다.
10만에 닿기 전에 이 문서를 다시 열 사람을 위한 것이다.

**측정** (Coway 2,377건, 2026-08-06. 10만 환산 배율 42.1)

| | 현재 | 10만 | 성격 |
|---|---|---|---|
| 샤딩 디렉터리당 | 24 | 1,000 | **무관** — 5만개 넣어도 개당 0.038ms (D11) |
| `ALIASES.md` grep | 80ms | 80ms | 속도는 무관 |
| └ **흔한 말 매치** | ~130줄 | **5,687줄** | **컨텍스트 범람 — 로컬판이 여기서 죽는다** |
| `build` 전량 재파싱 | 29.9초 | **21분** | 증분 없음, 싱크마다 |
| 서버 재색인 | 6.7초 | 5분 | 기동 시마다 (2026-08-06 부터) |
| 볼트 디스크 | 40MB | 1.7GB | 로컬판은 **1인당** |

**깨지는 순서**

1. **로컬판이 가장 먼저, 그리고 완전히 죽는다.** 스킬은
   `Grep(pattern, path="<VAULT>/ALIASES.md", output_mode="content")` 로 시작하는데,
   매치 5,687줄이 통째로 컨텍스트에 들어간다(줄당 80자면 450KB). 검색이 느려지는 게
   아니라 **답을 못 한다.** 지금은 흔한 말도 130줄이라 보이지 않는 문제다.
   서버판은 BM25 가 상위 N 개만 주므로 애초에 이 문제가 없다 — **10만은 서버판의 규모다.**
2. **`build` 21분.** 여기가 `mirror/structure/` 가 되살아나는 자리다(D7). 무엇이
   실제로 바뀌었는지 골라내지 못하면 싱크마다 21분을 낸다.
3. **서버 기동 5분.** 오늘 넣은 기동 적재(D-없음, `vaultBootstrap`)가 2,377건에선
   6.7초라 공짜지만 그 규모에선 아니다. **ACL 만 적재하고 색인은 디스크 것을 쓰는
   분기**가 필요해진다 — 지금 전체 재구축을 하는 이유는 "서버가 내려간 사이 sync 가
   돌았을 수 있다" 하나뿐이므로, 그 조건을 따로 감지하면 된다.

**그래서 10만에서 할 일은 샤딩이 아니라 이 셋이다:** (a) 증분 build, (b) 기동 시
조건부 적재, (c) 로컬판을 그 규모에 쓰지 않는 것.

---

## 기각된 소소한 것들

- **RRF에 신뢰도 부스트** (`RRF × (1+α·rel)`) — 합성 시뮬레이션에서 **3.3% 더 나빴다.**
  신뢰도를 정답 확률과 일치시켜 최대한 유리하게 만들었는데도 졌다. 억지로 튜닝해
  살리지 않았다. RRF 유지. (근거였던 `bench/`는 2026-08-04 제거돼 재현 불가 —
  게다가 그 시뮬레이션의 RRF는 순위만 쓰는 형태라 지금 배선된 신뢰도 가중 융합과
  다르다. 이 결론을 다시 근거로 삼으려면 실측부터 해야 한다.)
- **고정 IDF 임계** — BM25 IDF는 소규모 코퍼스에서 압축된다(문서 4개면 df=1 항의
  IDF가 1.20, 10개면 1.99). 코퍼스 크기에 연동해야 한다.
- **싱크 레이어 자체 구현** — `confluence-markdown-exporter`가 있지만,
  원본 XHTML 보존과 구조화된 앵커 추출이 필요해 결국 직접 구현했다.
  다만 **Kotlin으로 재구현하는 것은 기각** — Python 것이 이미 검증돼 있다.
- **Go** — 서버가 카운터만 들던 시점엔 유효했으나, 색인이 돌아오면서 한국어 형태소
  분석(Lucene Nori)이 필수가 됐다. Go의 유일한 이점인 단일 바이너리가
  cgo mecab 때문에 사라진다.

---

## 정체 규정

**RAG가 아니다.** 반환값이 내용이 아니라 좌표이고, 검색이 생성이 아니라 행동 선택을
조건화하며, 인덱스가 시스템 자기 동작의 산출물이다. RAG로 부르면 벡터 DB·청킹·
검색 정밀도 지표라는 반사가 따라오는데 이 도메인에서는 전부 어긋난다.

**LTR도 아니다.** 특징 벡터도 모델도 손실 함수도 없다. LTR의 **입력(특징)을 만드는
단계**이고, 지금 랭커는 RRF(비지도)다. 궤적이 수십만 건 쌓이고 특징 파이프라인이
생기면 그때가 LTR로 졸업할 시점이다. 다만 LTR은 `n`을 줄이므로 **L2와 대체 관계**다.

가까운 계보는 **에이전트 절차 기억** — AWM(ICML'25), APC(NeurIPS'25).
엄밀히는 절차보다 **일화 기억**에 가깝다.
