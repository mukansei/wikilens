# Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.venv/
# IDE — 개인 설정은 무시하되 실행 구성은 팀과 공유한다.
# (git 은 제외된 디렉터리로 내려가지 않으므로 `.idea/*` 로 열어둔 뒤 예외를 준다)
.idea/*
!.idea/runConfigurations/
# Kotlin / Gradle
.gradle/
build/
# 런타임 산출물 (루트 기준으로만 — contracts/shared-fixtures/ 아래 체크인된 정본 볼트는 무시하면 안 됨)
/mirror/
/derived/
/ALIASES.md
/TREE.md
mirror-root
.wikilens/
.wikilens-server/
