# WikiLens

Confluence 위키를 미러링하고 **앵커 텍스트**(다른 문서들이 이 페이지를 부르는 이름)를
색인해 어휘 격차를 메운다. 그 위에 에이전트 탐색 궤적을 축적하는 학습 레이어를 얹는다.

**특정 회사 전용이 아니다.** Cloud·Server/DC 양쪽, 인증 4방식을 지원한다. 저장소의
`Acme 2,383건` 류는 **측정 출처 라벨**이지 종속이 아니며, 배포되는 플러그인·CLI 에는
회사 고유값이 없다(전에 `setup` 이 만드는 `env.sh` 에 새고 있었다). 언어는 한국어
하나를 기본으로 한다 — `--wikilens.analyzer=korean|english|standard` 로 **색인 시점에**
고른다. **질의는 설정이 아니라 색인에 기록된 분석기를 쓴다** — 설정을 따라가면 그
사이가 조용히 0건이 되므로, 불일치가 성립하지 않게 만들었다. 둘은 재색인에서 만난다.
자세한 범위는 `README.md`.

```
cli/       Python  싱크·파싱·앵커 전치·로컬판        pytest
server/    Kotlin  Lucene/Nori 색인 + 학습 레이어    JUnit · 배선 검증됨(Acme 실데이터)
plugin/    local=스킬+커맨드 · client=MCP 도구 4개   tests/ 는 배포 밖 · 안내 포함
contract/  교차 언어 계약 + 공유 픽스처
docs/      아키텍처 · 임베딩 설계 제안
```

[`DECISIONS.md`](DECISIONS.md) — 뒤집힌 결정과 지우면 안 되는 것들.
**되돌리기 전에 읽을 것.**

## 먼저 실행할 것

```bash
./check.sh
```

계약(교차 언어) · pytest(cli+plugin) · MCP 프록시(pytest 가 안 걷는 독립 스크립트) ·
JUnit 넷을 돌리고 **한 줄로 판정**한다. 종료 코드가 실패 개수다.

**판정을 눈으로 하지 않는다.** 각 도구의 종료 코드로 보고 성공·실패를 같은 모양
(`PASS`/`FAIL`)으로 낸다 — 예전에는 출력을 grep 해서 판정했고, 성공만 라벨이라
`BUILD FAILED` 한 줄이 다른 출력에 묻혀 **실패한 채로 커밋한 적이 있다**(2026-08-08).

**명령 목록을 여기 옮겨 적지 말 것** — `check.sh` 가 정본이고 IntelliJ 실행 구성
"3. 전체 검증"도 그것을 부른다. 예전에는 이 목록이 두 곳에 있었다.

`shared_contract.sh`가 특히 중요하다. Python과 Kotlin이 파일로만 연결되어 있어
아래 계약이 어긋나면 **테스트는 통과하는데 런타임에 조용히 틀어진다.**

**개수는 일부러 안 적는다** — 늘 때마다 낡고, 돌리면 바로 나온다.

**변경 후 반드시 `./check.sh` 가 통과하는지 확인할 것.**

---

## 절대 깨면 안 되는 계약

Python과 Kotlin이 **파일로만** 연결되어 있다. 아래를 바꾸면 양쪽을 동시에 고쳐야 하고,
안 그러면 **에러 없이 조용히** 동작이 틀어진다.

| 계약 | 위치 | 깨지면 |
|---|---|---|
| 페이지 ID가 식별자 (제목 아님) | `layout.py`, `vault/Layout.kt` | 이름 변경 시 학습 전량 손실 |
| 샤딩 `{id뒤2}` (앞자리는 엔트로피 낮음) | 같음 (Kotlin 정의처는 `Layout.kt` 한 곳뿐) | Kotlin이 파일을 못 찾음 |
| `canonical_json` 결정적 직렬화 | `models.py` | 매 싱크마다 전 파일이 변경으로 잡힘 |
| `anchors.jsonl` 스키마 | `build.py` ↔ `VaultReader` | 앵커 색인 유실 |
| `ancestors` 스키마 (id/title 목록) | `sync.py` ↔ `VaultReader.readAncestors` | 서버 트리가 조용히 평평해짐 |
| 빌드 멱등성 (2회 실행 = 바이트 동일) | `tests/test_build.py` | 무효화 폭풍 |
| 항 단위 포스팅 (키워드 **집합** 아님) | `TrajectoryStore` | 표현이 다르면 카운트 분산 |
| 사전확률 클램프 `[0.05, 0.85]` | `learn/Reliability.kt` | 1관측에 신뢰도 1.0 |
| `LOCALIZATION`만 간선 생성 | `learn/Gate.kt` | 경로 의존 질의에 틀린 답 |
| 질의 분석기는 **색인 기록**을 따름 (설정 아님) | `LuceneIndex.swap` ↔ `ANALYZER_KEY` | **검색이 에러 없이 0건** |
| 자격증명 파일 경로가 세 곳에서 같음 | `credentials.py` · `vault_status.py` · `wikilens_cli.sh` | CLI 는 읽는데 진단은 "없다" |
| 볼트 설정 키 `vault` 가 세 곳에서 같음 | `vault_status.py` · `setup_vault.py` · `config/UserConfig.kt` | 서버 볼트 폴백이 조용히 멈춤 |
| 볼트 경로 해석처가 **한 곳** | `vault/VaultLocator.kt` | 검색은 되고 읽기만 404 |
| grep 엔진 둘이 같은 답 | `GrepEngineParityTest` | 판을 옮기면 다른 답 |
| grep 엔진은 ACL 을 모름 | `ContentService` 가 걸러 넘김 | 한쪽이 조용히 더 보여줌 |
| 권한 없음은 **404** (403 아님) | `Controller.read` | 문서 존재가 유출됨 |
| 관리 API 기본이 **잠김** | `WikiLensProperties.adminToken` ↔ `application.yml` | 조용히 열린 채 배포됨 |
| 관리 API 는 **경로로** 잠김 | `AdminGuardConfig.ADMIN_PATHS` | 새 엔드포인트가 열린 채 추가됨 |
| ACL 파일 경로·형식 | `cli/wikilens/acl.py` ↔ `VaultReader.readAcl` | 전 페이지가 `@public` 폴백 |
| ACL 조회 실패는 **옛 값 유지** | `acl.py` `collect` | 못 읽은 페이지가 공개로 |
| 상속은 **못 읽은 조상에서 멈춤** | `acl.py` `collect` | 잠긴 부모 밑이 `@space` 로 |
| acl.json 에 **없는 항목 ≠ 수집 안 함** | `VaultReader.readAcl` | 파이썬 fail-closed 가 뒤집힘 |
| 학습 힌트는 **자르기 전에** 권한으로 거름 | `TrajectoryStore.hints` ↔ `SearchService` | 좁은 권한은 힌트가 0건 |
| 궤적에 남기는 것은 **범위**이지 신원 아님 | `Trajectory.scope` ↔ `AclRegistry.scopeOf` | 검색 이력이 영구 기록됨 |
| 상태 디렉터리는 **한 프로세스만** | `learn/StateDirLock.kt` | 학습이 조용히 갈림 |

---

## 조용히 실패하는 것들

에러 없이 잘못 동작하므로 **테스트로만 잡힌다.** 전부 실제로 겪은 버그다.

1. **토크나이저 불일치** → 힌트가 항상 0건. 클라이언트와 서버가 같은 규칙을 써야 한다.
   서버판에서는 서버가 유일한 토크나이저이므로 클라이언트는 원문만 보낸다.
2. **키워드 집합을 키로 사용** → "로그인 붙이는 법 어디"와 "로그인 붙이는 법"이
   다른 키가 되어 5세션이 각각 1회로 흩어진다. 자연어 질의는 매번 표현이 다르다.
3. **사전확률 1.0** → Beta 한쪽 모수가 0 → 관측 한 번에 신뢰도 1.0.
4. **이중 확정** → `onQuery`가 확정한 스팬을 `onEnd`가 또 확정. `finalized` 플래그 필수.
5. **도메인 명사를 흐름 마커로** → "배포 파이프라인 문서 어디"가 TRACING으로 오탐.
   흐름을 *묻는* 표현만 마커에 넣는다.
6. **401을 "API 없음"으로 해석** → 토큰 문제를 URL 문제로 오진.
   `detect_prefix`는 401/403을 "찾음"으로 취급한다.
7. **Gate 폴백 임계값이 너무 타이트했음** → 마커에 안 걸리는 자연어 질의는 3토큰 넘으면
   전부 UNKNOWN이라 학습 간선이 거의 안 생겼다(실측: 7토큰 질의가 `success=true`인
   궤적으로 기록됐는데도 `postings`는 안 늘어남). 임계값을 8로 올리고 `RATIONALE`
   마커에 "배경"을 추가해 대응(`Gate.classify`). 마커 체크가 길이 폴백보다 먼저
   실행되므로 RATIONALE/TRACING은 마커가 커버하는 한 길이와 무관하게 안전하지만,
   마커에 안 걸리는 긴 경로의존 질의를 오분류할 잔여 위험은 남는다 — `pWrong`으로
   모니터링할 것.
8. **학습 전용 힌트가 통째로 버려졌음** → `SearchService`가 메타데이터를 어휘 결과로만
   채우고 거기 없는 후보를 걸러내, **어휘 검색이 못 찾고 학습으로만 발견된 페이지가
   항상 폐기**됐다. 학습 레이어의 존재 이유가 죽어 있었고 `SKILL.md`가 문서화한
   `S = 학습 힌트만` 마커는 도달 불가능한 분기였다. 색인에 없으면 `index.metaOf()`로
   폴백하고, `take(limit)`을 필터 **뒤로** 옮겨 해결(안 그러면 버려질 후보가 슬롯을 먹음).
   `SearchService`에 테스트가 하나도 없어서 오래 살아남았다.
9. **자격증명이 환경변수 전용** → `export`는 그 셸에서만 산다. 로컬판은 래퍼로
   막았는데 **서버판 cron 은 안 막혀 있었다** — 문서 네 곳이 안내하는 자동 싱크가
   `CONFLUENCE_URL 환경변수가 필요합니다` 로 죽는다(실측: `env -i` 로 재현). `&&` 가
   절반 반영은 막지만 **볼트가 낡아가는 걸 알 방법이 없다.** 이제 CLI 가
   `credentials.py` 에서 환경변수 → `~/.wikilens/env.sh` 순으로 읽는다 — 읽는 자리가
   하나여야 한쪽만 고쳐지는 일이 안 생긴다. 아래는 그 원본 서술이다.
   **설정이 환경변수 전용** → `export`는 그 셸에서만 산다. 로컬판은 검색만 되고
   `sync`가 조용히 죽고(사용자는 정상인 줄 알고 수동 싱크를 하고 있었다), 서버판은
   앱으로 띄우면 주소가 localhost 기본값이 돼 **전 결과가 빈다.** 정본은
   `~/.wikilens/` — 비밀 아닌 설정은 `config.json`, 토큰류는 `env.sh`(600).
   CLI 경로도 여기 적는다(venv·pipx 는 PATH 에 없다). 근거는 `DECISIONS.md` D10.
10. **서버 ACL에 사용자가 0명이면 색인이 멀쩡해도 전원이 빈손** — fail-closed라
   맞는 동작인데 "문서가 없다"와 구별되지 않는다. **이제 끌 수 있다**
   (`--wikilens.acl-enforced=false`) — 지금의 시행은 `sync` 가 권한을 안 가져와
   실질적으로 사용자 허용목록이라, 신뢰 경계 안에서는 얻는 것 없이 함정만 됐다.
   기본은 켜짐이고 꺼두면 기동 로그·`/api/stats`·`--status` 셋이 계속 말한다.
   **스위치는 `AclRegistry` 한 곳**이다 — 소비자가 각자 분기하면 한 곳이 빠져
   반쪽으로 열리고 그건 겉으로 정상이다. 계약이 검사한다. 실측으로 인과 확인(같은 질의:
   등록 전 0건 → `@public` 등록 후 3건). `wikilens_mcp.py --status`가 주소·식별자·
   도달·색인·ACL을 한 번에 보여준다 — `/api/health`·`/api/stats`는 원래 있었는데
   플러그인이 안 써서 사용자에게 안 닿았다.
11. **버전을 안 올리고 소스만 고침** → 설치는 버전별 캐시로 **복사**되므로 캐시가
   그대로 남아 설치된 플러그인이 조용히 구버전으로 돈다. 버전 번호가 같으니 아무도
   의심하지 않는다. 실측: 소스에선 CLI 경로 해석이 되는데 설치본은 `CLI=`가 비고
   래퍼가 CLI를 못 찾았다(7개 파일이 달랐다). **판정은 저장소가 아니라 설치본 기준.**
   계약이 설치본과 소스를 실제로 비교한다.
12. **서버가 재기동하면 검색은 되는데 읽기가 전부 404였음** — 기동 훅이 없어 볼트를
   읽는 건 `/admin/reindex` 때뿐인데, Lucene 색인은 디스크에서 되살아나고 ACL 페이지
   맵은 순수 힙이라 빈 채로 떴다. `search` 는 Lucene 필터를, `read` 는 `acl.canSee` 를
   쓰므로 **둘이 어긋났다.** 겉으로는 전부 초록이었다(실측: `indexedDocs=2377`·
   `aclPages=0`, read 404). `vaultBootstrap` 빈이 기동 시 적재하도록 고쳤다 —
   `ApplicationRunner` 가 아니라 **빈 생성 시점**이어야 웹 서버가 포트를 열기 전에
   끝난다. `--status` 도 이 조합(`docs>0 && pages==0`)을 짚는다.
   **남은 구멍: 사용자 등록(`byUser`)은 여전히 메모리 전용이다** — 재기동하면 전원이
   사라져 운영자가 다시 등록해야 한다. ACL 수집(다음 작업 1번)과 함께 풀 것.
13. **서버 `grep` 이 세 가지로 조용히 틀어졌었다** — 사용자 정규식을 `java.util.regex`
   로 그대로 실행해 요청이 안 끝나거나(`(.+)+@@@@`) 스택을 넘겨 HTTP 500 이 됐고
   (`(a|aa)+c`), 깨진 UTF-8 파일 하나가 스캔 **전체**를 500 으로 죽였으며, `regex`
   토글이 문법이 아니라 **대소문자 민감도**를 바꿨다. 전부 고쳤다 —
   엔진은 RE2(`com.google.re2j`), 디코더는 `REPLACE`, 매칭은 `CASE_INSENSITIVE`.
   **되돌리면 `ContentServiceTest` 가 8건 빨개진다**(실제로 되돌려 확인). 근거는
   `DECISIONS.md` D12, 두 판이 대소문자를 함께 무시하는 것은 계약이 지킨다.

14. **빈 볼트가 멀쩡한 색인을 지웠다** — 기동 적재가 `rebuild(emptyList())` 를 무조건
   불러서, 볼트 경로 하나 잘못 준 재기동이 **마지막으로 성공한 색인까지 0건으로
   덮어썼다**(실측: `색인 재구축 0건`). 볼트를 못 읽는 건 고치면 되지만 지워진 색인은
   재싱크해야 한다. 이제 비면 재색인을 건너뛰고 옛 색인으로 계속 서빙한다.
15. **버려진 세션의 궤적이 학습에 안 들어갔다** — `sweep()` 은 있는데 **부르는 코드가
   없었다.** `/api/admin/sweep` 수동 하나뿐이었고 세션 종료는 MCP 프록시의 `atexit` 에만
   걸려 있어, SIGKILL·크래시면 `onEnd` 가 영영 안 온다. 맵 누수보다 **궤적 미확정이 더
   나쁘다** — 사용자는 정상적으로 검색·읽기를 했는데 배운 게 없다. `SessionSweeper` 가
   5분마다 거둔다(실측: idle 2초·주기 3초로 `activeSessions` 1→0, `trajectories` 38→39).
16. **궤적 로그가 조용히 갈라졌다** — 상태 디렉터리 기본값이 상대경로라 작업 디렉터리가
   바뀌면 다른 자리를 가리키는데, `replayInto` 가 파일이 없으면 **로그 한 줄 없이 0을
   반환**했다. 그러면 옛 궤적을 못 읽은 채 새 로그를 쌓는다 — 사라지는 게 아니라
   **고아가 된다**(궤적은 유일한 복구 불가 자산). IntelliJ 가 `fun main` 에서 바로
   띄우면 작업 디렉터리가 저장소 루트라 정확히 이렇게 된다(실측). 이제 기존 로그가
   없으면 WARN 한다 — "처음이면 정상, 재기동이면 갈라진 것".
17. **기록 없는 옛 색인에 설정을 적용했다** — 분석기 기록을 남기기 전에 지어진 색인은
   전부 korean 인데, 기록이 없으면 **설정을 따랐다.** `english` 로 띄운 순간 옛 korean
   색인을 english 로 두드린다. 그 분기는 "색인은 있는데 기록만 없는" 경우뿐이라
   설정이 한 번도 맞지 않는다 — `LEGACY_KIND` 로 고정하고 WARN 한다. 재색인 한 번이면
   기록이 생겨 안 쓰인다.
18. **빈 스냅샷이 분석기를 korean 에 고정했다** — 정적 상수라 `buildKind` 를 몰랐다.
   색인이 아직 없으면 `--wikilens.analyzer=english` 를 줘도 **질의만 korean 으로 분석**된다.
   검색은 어차피 0건이라 안 보이지만 그 항이 학습 포스팅의 키다.
19. **볼트 경로를 푸는 곳이 둘이었다** — `IndexingService`(색인·ACL)와 `ContentService`
   (read·grep)가 각자 `props.vaultRoot` 를 풀었고, 후자는 `toAbsolutePath()` 조차 안 걸어
   **실행 디렉터리에 매달려** 있었다. 설정 폴백을 넣자 갈림이 결정적이 됐다 — 실측:
   폴백으로 기동한 서버가 문서 3건을 색인하고 **검색까지 정상인데 read 는 전부 404**.
   12번과 같은 실패 모양인데 원인만 다르다. `VaultLocator` 하나로 합쳤다.
   **합친 뒤에 딸려온 함정:** 해석이 공짜가 아니게 됐다(폴백이면 stat 두 번 +
   `config.json` 파싱). `grep` 이 문서마다 부르고 있어서 스캔에 10%가 얹혔다
   (실측: 2,383회 66ms, grep 전체 0.64초). 요청당 한 번만 풀고, 공짜처럼 보이던
   `root` 프로퍼티는 아예 없앴다 — 주석보다 그쪽이 확실하다.
20. **macOS JDK 가 `HOME` 을 무시한다** — `user.home` 은 `getpwuid` 결과다(실측:
   `HOME=/tmp/x java …` → `user.home=/Users/hyunwpark`). 파이썬 `Path.home()` 은 `HOME`
   을 먼저 보므로, 그대로 두면 **CLI 가 쓴 `~/.wikilens/config.json` 을 서버가 다른
   자리에서 찾는다.** 서버는 systemd·cron 처럼 `HOME` 이 다르게 잡히는 환경에서 도는
   물건이라 가정이 아니다. `UserConfig.defaultHome()` 이 `HOME` 을 우선한다.
21. **깨진 `config.json` 위에 얹어 쓰면 원본이 통째로 사라졌다** — `_config()` 가 파싱
   실패를 `{}` 로 돌려주는데 그 위에 새 값을 저장했다. 이 파일은 **사람이 손으로 고치는
   파일**이라 깨져 있는 것이 예외가 아니고, **두 판이 공유**하므로 한쪽이 지우면 다른
   판의 설정까지 날아간다. 실측: 쉼표 하나가 잘못된 파일에 `--configure` 한 번 →
   `vault`·`cli` 소실, 그런데 출력은 "설정했습니다". 이제 `config.json.bak-<시각>` 으로
   치워두고 알린다(`quarantine_unusable_config`).
   **판정 기준이 "파싱된다" 여서는 안 된다** — `null`·`[]`·`"문자열"` 은 전부 유효한
   JSON 이라 통과한 뒤 `.get()` 에서 AttributeError 로 터진다. 로컬판은 진단
   스크립트가 죽어 스킬이 `VAULT=` 대신 traceback 을 받고(**스킬에 그 분기가 없어
   검색 불가**), 서버판은 그 자리가 모듈 최상단이라 **프록시가 기동 중 죽어 도구
   4개가 통째로 사라진다.** `isinstance(cfg, dict)` 까지 봐야 한다.
22. **학습 힌트를 자른 뒤에 권한으로 걸렀다** — `hints()` 가 `take(limit)` 을 먼저 하고
   `SearchService` 가 그 결과를 ACL 로 걸러서, 권한이 좁은 사용자는 상위 5개가 전부
   안 보이는 문서면 **힌트가 통째로 0** 이 된다(볼 수 있는 후보가 아래에 있어도 슬롯을
   이미 뺏겼다). 8번과 **같은 실패인데 자리만 다르다** — 그때 `SearchService` 만 고치고
   `hints()` 는 그대로 뒀다. 전 페이지가 `@public` 이라 안 보이고 **ACL 수집이 들어오는
   순간 나타난다.** 술어를 `hints()` 안으로 넣어 해결(순수 함수라 `learn/` 무의존 유지).
23. **상태 디렉터리에 단일 쓰기 보증이 없었다** — Lucene `write.lock` 은 `rebuild` 동안만
   잡히므로(`IndexWriter(...).use { }`) 그 밖의 시간에는 둘째 서버가 그냥 뜬다. 붙으면
   각자의 힙에 다른 포스팅을 들고 **같은 궤적 로그에 append** 한다 — 같은 질의가 어느
   프로세스에 붙느냐로 답이 달라지고 `/api/stats` 도 두 값을 오간다. `StateDirLock` 이
   기동을 막는다(볼트를 못 읽는 경우와 달리 **여기서는 막는 게 맞다** — 이미 정상
   동작 중인 서버가 있으므로 진단 경로가 사라지지 않는다).
24. **궤적 로그 쓰기 실패가 WARN 한 줄이었다** — 실패해도 `apply` 는 계속 돌아 **메모리
   학습만 앞서간다.** 재기동하면 그만큼이 사라지는데 유일한 복구 불가 자산이다.
   이제 세어서 `/api/stats` 의 `logWriteFailures` 와 `--status` 가 짚는다.
25. **`.mcp.json`이 미설정 env 를 리터럴로 주입** → 변수가 없으면 `"${WIKILENS_SERVER}"`
   문자열이 그대로 들어가 프록시 기본값을 덮어쓰고 `unknown url type` 으로 죽는다.
   env 를 넘기지 않고 프록시가 `os.environ` 에서 직접 읽게 한다.

---

## 의도적으로 이상해 보이는 것

되돌리기 전에 이유를 확인할 것.

- **`server/src/main/.../learn/`에 Spring·Lucene import가 없다** — EB·게이트·궤적은
  순수 알고리즘이고, 프레임워크가 새어 들어오면 랭킹·색인 관심사와 뒤엉켜 단위
  테스트가 통합 테스트로 변질된다. `shared_contract.sh`가 강제한다. import 추가 금지.
- **서버가 Confluence를 크롤하지 않는다** — Python `sync`가 이미 검증돼 있다.
  서버는 `mirror/`를 읽기만 한다. 재구현하지 말 것.
- **`sync`와 `build`가 분리** — 제목→ID 해석 완전성 때문. 원본을 다 받은 뒤
  한 번에 파싱해야 링크 해석률이 100%가 된다.
- **궤적 로그를 압축하지 않는다** — append-only 라 줄지 않지만 실측(2026-08-08)에서
  100만 건이 210MB · 재생 5.3초 · 힙 80MB 였고, 20명 팀이면 그게 **7년치**다. 처음
  아픈 지점은 1,000만 건(52초)이다. 압축은 이르기도 하지만 **유일한 복구 불가 자산에서
  무엇을 버릴지 정하는 일**이라 위험하다. 대신 `/api/stats` 의 `trajectoryLog` 와 기동
  경고(재생 10초 초과)로 **필요해지는 시점을 서버가 알리게** 했다. 그때 만들 것은
  삭제가 아니라 체크포인트다 — `DECISIONS.md` D17.
- **증분 색인을 안 한다** — 전체 재구축이 **13,921건에 14.8초**다(실측 2026-08-08,
  실제 코퍼스). 예전의 "2,377건 6.7초 → 10k 환산 28초" 외삽은 **2.6배 비관적이었다** —
  문서당 비용이 규모에 따라 줄어든다. 증분은 드리프트가 조용히 쌓이는 자리라 이득이 없으면 피한다.
  **코퍼스가 10만에 닿으면 이 판단이 뒤집힌다** — 그때 먼저 깨지는 것들을
  `DECISIONS.md` D12 에 측정해 뒀다(로컬판이 가장 먼저, build 21분, 기동 5분).
- **본문 스캔 경로가 둘이다(JVM·ripgrep) — 대조로 다룬다.** rg 가 없는 머신이 있어
  폴백이 영구히 공존한다. 갈리는지 모르는 채로 두는 대신 `GrepEngineParityTest` 가
  14개 패턴으로 두 엔진의 매치를 대조한다(`-i` 를 빼거나 ACL 필터를 지우면 실제로
  빨개진다). **ACL 은 엔진 밖**에 있고(`ContentService` 가 거른 목록만 넘긴다),
  응답의 `engine` 이 어느 경로였는지 말한다. 근거는 `DECISIONS.md` D20.
- **`grep` 정규식이 `java.util.regex` 가 아니라 `com.google.re2j` 다** — 표준 엔진은
  재귀 백트래킹이라 사용자 패턴 하나로 스레드가 묶이거나(`(.+)+@@@@`) 스택을 넘겨
  500 이 났다(`(a|aa)+c`). 상한 셋으로 증상을 재다가 엔진을 바꿨다. 역참조·전방탐색을
  잃지만 ripgrep 도 같은 제약이고, **나중에 rg 를 속도용으로 붙일 때 두 경로가 갈리지
  않는 근거**가 된다. 되돌리면 그 두 결함이 함께 돌아온다 — `DECISIONS.md` D12.
- **두 판 다 대소문자를 무시한다 — rg 기본값을 일부러 거스른다** (로컬은 `Grep(-i=true)`,
  서버는 `Re2.CASE_INSENSITIVE`). 한국어 문서에 영문 고유명사가 섞여 있어 사용자가 어떤
  표기로 칠지 알 수 없다 — 실측: `acme` 가 구분 17건 · 무시 48건. 한쪽만 되돌리면
  **판을 옮긴 사용자가 같은 질의에 다른 답을 받는다.** 계약이 둘을 함께 검사한다.
- **한 파일에 한 선언이다** — Kotlin 공식 스타일은 "밀접하면 한 파일에 둬도 좋다"지만,
  이 저장소는 **파일명으로 찾는 쪽**을 택했다. 대가는 파일 수(15 → 46)이고,
  얻는 것은 `Gate` 를 고치려는 사람이 `Scoring.kt` 를 열지 않아도 되는 것이다.
  **계약이 파일 경로를 grep 하므로 파일을 나눌 때 계약도 함께 고쳐야 한다** —
  실제로 이 분할에서 셋이 깨졌다(`learn/Scoring.kt` 가 `Gate.kt`·`Reliability.kt`·
  `QueryKind.kt` 로 나뉘며 사라졌다).
- **`grep` 은 궤적을 안 남긴다** — `search`·`read` 와 달리 `store` 를 안 부르고
  `GrepRequest` 에 `sessionId` 도 없다. 포스팅의 키가 분석된 항인데 grep 패턴은 토큰
  경계와 무관한 부분문자열·정규식이라 섞으면 키가 오염된다. 대가는 **grep 으로 찾아
  읽은 것이 학습에 안 들어가는 것**이고(열린 스팬이 없어 `onRead` 가 버린다) 의도한
  손실이다. `tree` 를 뺀 것과 같은 계열이다.
- **`api/` 와 `service/` 가 나뉘어 있다** — `api/` 는 HTTP 표면(Controller·Dto)이고
  서비스는 `service/` 다. 다만 **Dto 는 `api/` 에 남아 서비스가 그대로 반환한다** —
  타입이 넷뿐이라 매핑 층은 얻는 것 없이 코드만 두 배가 되고, 이 서비스들은 HTTP 말고
  소비자가 없다. 소비자가 생기면 그때 나눌 자리다(`api/Dto.kt` — 선언 없이 패키지 설명만 둔 파일).
- **`--enable-native-access=ALL-UNNAMED` 는 성능이 아니라 로그 때문이다** — Lucene 이
  `posix_madvise` 를 FFM 으로 부르는데 Java 22+ 가 경고 넷을 찍는다. 빼도 동작한다
  (실측: 차단해도 색인 2,383건 정상, 시간 차 노이즈). 기동 로그를 진단에 쓰는 구조라
  무해한 경고가 정작 볼 줄(궤적 갈림 WARN 등)을 가리는 것이 이유다.
- **CLI 를 `~/.wikilens/venv` 에 설치한다 — 어디에 깔렸는지 찾지 않는다.** 예전에는
  `pip install` 뒤에 찾는 단계가 따로 있었고(`--cli-path auto`·`discover_cli()`),
  그 불확실성 하나를 다루는 장치가 코드 넷·문서 셋이었다. **자리를 정하면 그 전부가
  한 줄이 된다.** 볼트·설정과 같은 디렉터리인 것도 의도다 — 지우는 방법이
  `rm -rf ~/.wikilens` 하나로 유지된다. 근거는 `DECISIONS.md` D15.
- **래퍼가 `--root` 를 채우고, CLI 는 서브커맨드 뒤에서도 `--root` 를 받는다** —
  전자는 볼트 경로 정본을 아는 자리가 이미 래퍼여서고, 후자는 함정을 경고하는 것보다
  없애는 편이 싸서다(같은 경고가 문서 4곳에 복제돼 있었다). 사용자가 직접 준 값이 이긴다.
- **서버가 `~/.wikilens/config.json` 을 읽는다** — 심링크를 손으로 만드는 단계를
  없앤다. **명시 설정이 항상 이기고**, 폴백은 기본 경로가 비었을 때만 걸린다.
  D10 이 CLI·플러그인까지만 통일했던 것을 서버까지 끌고 온 것이다(D15).
- **가상 스레드를 껐다 — 재봤고 차이가 없었다.** 실제 코퍼스 13,921건에서 켬/끔을
  번갈아 세 번 쟀는데(2026-08-08) **모든 항목에서 ON 값이 OFF 두 번의 범위 안에 들었다.**
  OFF 자기 재현 편차가 12~41% 인데 ON/OFF 차이가 그보다 작다. 얻는 게 없으면 단순한
  쪽(기본값)을 두고, 안전성이 JDK 24+ 에 걸리는 의존도 함께 지운다.
  **측정에 두 번 속았다** — 첫 대조는 ON 을 먼저 재서 캐시가 차가웠고(같은 ON 을 뜨거운
  상태로 다시 재니 11,910 → 19,412), 첫 측정기는 요청 수가 적어 부호가 오락가락했다.
  순서를 뒤집어 대조하지 않았으면 "ON 이 37~59% 느리다"를 결론으로 냈을 것이다.
  수치와 되돌릴 조건은 `application.yml` 주석에.
- **세션 거두기 경쟁을 잡지 않았다 — 재보고 그만뒀다.** `computeIfAbsent` 와 락 사이에
  스위퍼가 세션을 거두면 고아 객체에 스팬이 쌓여 그 궤적이 확정되지 않는다. "락 안에서
  맵의 주인인지 재확인" 하는 방어를 만들어 대조했더니 **재확인 있음 1967 · 없음 1977**
  (2,000 라운드 × 5회, `idleMillis=0` 최악 조건) — 줄이기는커녕 조금 나빴다. 손실의
  주범이 다른 것이기 때문이다: 스위퍼가 `onQuery` 와 `onRead` **사이**에 끝내는 경우이고
  그때 빈 스팬을 버리는 것은 **정당한 동작**이다. `SessionRaceTest` 가 그 측정 장치다 —
  되살리려면 거기서 먼저 잴 것.
- **`server/mirror-root` 는 심링크다** — gitignore 대상이라 clone 하면 없다.
  사본을 만들면 볼트가 둘이 되어 어느 쪽을 색인했는지 알 수 없다(`server/README.md`).
- **`mirror/structure/`를 쓰는데 아무도 안 읽는다** — 지우지 말 것. 증분 build를
  도입하는 순간 필요해진다(`version`은 오탈자 수정에도 오르니 무효화 신호로 너무
  거칠고, 이게 "구조가 실제로 바뀐 페이지"만 골라낸다). 유지 비용은 362B/페이지 ·
  읽기 59ms. 근거는 `DECISIONS.md` D7.
- **`raw/` XHTML을 버리지 않는다** — 마크다운 변환은 매크로를 잃는다.
  변환기가 개선되면 재크롤 없이 재변환할 수 있다.
- **플러그인이 `wikilens-client`/`wikilens-local`, 디렉터리는 `plugin/client/`** —
  저장소의 `server/`가 진짜 Kotlin 서버라 플러그인이 그 이름을 쓰면 한 단어가 두
  물건을 가리킨다. 무표지 `wikilens`를 없애 어느 쪽도 "기본"을 참칭하지 않는다.
- **두 판이 "배타적" 이 아니라 서버판 우선이다** — 배타성은 강제할 수단이 없었다
  (이 머신에도 둘 다 켜져 있었고 아무 경고가 없었다). 서버판 MCP 도구는 스킬 선택과
  무관하게 항상 보이므로 로컬판이 이기게 만들 방법이 아예 없다. 대신 모델이 도구
  목록으로 즉시 판정할 수 있는 우선순위를 양쪽에 적었다 — `DECISIONS.md` D13.
- **모델용 지시 문서는 명령형 존댓말로 통일한다** (스킬 2 · 커맨드 2 · 레퍼런스 1).
  독자가 같으니 문체가 갈릴 근거가 없고, 무엇보다 **평서형은 지시로 안 읽힌다** —
  "못 찾았다고 말합니다" 는 일어나는 일에 대한 서술이라, 하필 환각 방지 규칙이
  주변의 사소한 지시(`쓰세요`·`마세요`)보다 약한 문형이었다. 계약이 검사한다.
- **스킬 이름은 플러그인 이름과 다르다(양쪽 다 `search`)** — 스킬은 `플러그인:스킬`로
  노출돼 같게 두면 `wikilens-local:wikilens-local`이 된다(실측). 대신 구별 근거가
  description 하나뿐이라 계약이 그것을 지킨다.
- **`marketplace.json`의 `renames` 는 지금 아무 일도 안 한다. 지우지 말 것** —
  플러그인 이름은 게시 후 **불변**이고(바꾸면 기존 설치가 `plugin-not-found`),
  그 유일한 탈출구다. 다음에 개명할 사람에게 적을 자리를 알리려고 남긴다.
  **계약이 못 잡는다** — 통째로 지우면 조용히 통과한다. 근거는 `DECISIONS.md` D9.
- **마켓플레이스 매니페스트가 저장소 루트에 있다** — `.claude-plugin/marketplace.json`.
  하위 디렉터리로 옮기면 플러그인 `source` 상대 경로가 잘못 해석돼 설치가
  `"source type your Claude Code version does not support"` 로 실패한다(실측).
- **로컬판 볼트 경로 정본이 `~/.wikilens/config.json` 이다** — 환경변수가 아니라.
  Claude Code 가 띄우는 셸에서 `export` 해도 다음 세션엔 없어서, env 단독으로는
  "어디서든 검색"이 성립하지 않는다. `WIKILENS_VAULT` 는 일회성 재정의로만 남겨뒀다.
- **로컬판은 MCP 를 안 쓴다** — 서버판과 달리 스킬 + 네이티브 grep 이다. 로컬판의
  정의적 성질이 "검색 경로 런타임 의존성 0"이라, 프로세스 기동이 끼면 파이썬 환경
  문제가 검색 실패로 나타난다. 근거와 전환 조건은 `DECISIONS.md` D8.
- **검사 장치가 둘이다** — `shared_contract.sh`(grep, 빠르지만 리팩터링에 취약)와
  `contract/shared-fixture/`(실제 파싱·생성 후 비교, 느리지만 동작을 잠금). 겹쳐 보이지만
  잡는 실패가 다르다 — grep 은 문자열만 바뀌어도 오탐이고, 픽스처는 포맷이 조용히
  갈라지는 것을 잡는다.

---

## 측정된 것 vs 가정한 것

**합성 코퍼스 시뮬레이션(`bench/`)은 제거됐다** (2026-08-04). 앵커 텍스트 효과·churn
민감도처럼 그 시뮬레이션에만 의존하던 수치는 근거 없이 남겨두지 않으려고 표에서
함께 지웠다 — 애초에 유효했던 건 절대 수치가 아니라 공식의 참/거짓·방향성·민감도뿐이었다.

| | 상태 |
|---|---|
| 손익분기 `p_hit > p_wrong/(n−1)` | 대수적 유도로 성립. **`1/n`은 틀린 공식**(한때 시뮬레이션으로 실측 일치까지 확인했으나 그 산출물은 제거됨 — 유도는 재현 가능) |
| EB 수치 | Python↔Kotlin 1e-6 일치 (`LearnLayerTest.kt` 의 기대값 6개가 `scoring_reference.py` 산출). **scipy 대조는 성립하지 않는다** — `scoring_reference` 가 scipy 를 일부러 안 쓰고(뉴턴법 자체 구현) venv 에도 없다 |
| 실사용 적중률 | **측정된 바 없음** |
| Kotlin Lucene/Spring 배선 | 빌드·bootRun·재색인 검증됨 (Acme 실데이터 2,378건, 2026-08). 검색 랭킹 **품질**은 별도 미검증 |

---

## 다음 작업 (우선순위)

**완료 목록은 여기 안 적는다** — `git log` 와 `DECISIONS.md` 가 더 정확한 출처이고,
여기 적으면 세션마다 늘어나면서 정작 "다음"이 묻힌다. 굵직한 것만: 실제 Confluence
연결(Acme CWDOMESTICDT), Kotlin 배선·기동 적재, 두 판 플러그인 설치 실동작,
로컬판 배포 가능화(D8), 설정 정본 통일(D10), 샤딩 교체(D11), "유용했다" 판정,
grep 엔진 RE2 전환(D12), 두 판 우선순위(D13), 분석기 색인 시점 선택(D14).

**지금 팀에 배포한다면 로컬판이다.** 서버판은 ACL 수집 전까지 제3자를 붙이면 안 되는데,
로컬판은 각자 자기 토큰으로 싱크해 그 문제가 설계상 없다. 대가는 Confluence 부하가
사용자 수에 비례하는 것이라 소규모 팀에 한한다.

1. **서버판 인증·ACL — 만들었다(2026-08-08). 남은 것은 운영 절차다.**
   - **`/api/admin/*` 은 공유 토큰으로 잠긴다**(`wikilens.admin-token`). **기본이 잠김**이라
     안 주면 전부 404 다 — 열림이 기본이면 조용히 열린 채 배포된다. 거부는 403 이 아니라
     404(엔드포인트 존재를 안 알린다). **이것으로 `userKey` 위조는 못 막는다** — 그건
     리버스 프록시(SSO)가 필요하고, 공유 토큰은 그 아래 깔리는 바닥이다(D18).
   - **`wikilens acl` 이 권한을 수집한다.** `sync` 와 분리돼 있다 — 권한 변경은
     `lastModified` 를 안 건드려 증분 sync 가 영영 못 잡는다. **더 자주 돌려야 한다.**
     `byOperation` 은 직접 제한만 주므로 `ancestors` 로 상속을 직접 푼다(안 그러면
     상속으로 잠긴 문서가 통째로 노출된다). 제한 없는 페이지는 `@public` 이 아니라
     `@space:<KEY>` 다 — 여러 스페이스를 한 볼트에 모으면 사용자마다 볼 수 있는
     스페이스가 다르기 때문이다.
   - **사용자 등록이 재기동을 넘는다**(`acl-users.json`, 원자 교체).
   - **① 토큰 불일치 진단 — 넣었다(2026-08-10).** `/api/stats` 의 `aclTokenOverlap`
     과 `--status` 의 `ACL_TOKEN_OVERLAP=0` 이 양쪽 토큰을 찍어 말한다. 아래가 그
     구멍의 서술이다.
     **남은 것 ① (해결됨) — 토큰 불일치가 진단되지 않는다.** `canSee` 는 조건이 둘인데
     (`등록됐나` + `토큰이 겹치나`) 진단은 첫째만 본다. **`wikilens acl` 을 처음 돌리는
     순간 이게 터진다** — 그전에는 전 페이지가 `@public` 폴백이라 `["@public"]` 로
     등록한 사람이 다 보이는데, 수집 후에는 페이지 토큰이 `@space:<KEY>` 로 바뀌어
     **전원이 0건**이 된다. 등록도 색인도 멀쩡하고 `--status` 는 `ACL_USERS=3` 으로
     초록이다. 권한 수집이라는 **옳은 조치가 전원 블랙아웃으로 나타나고 아무도 이유를
     모른다** — 조용히 실패 10번과 같은 계열이다.
     **고칠 방법은 싸다:** 사용자 토큰 합집합 ∩ 페이지 토큰 합집합이 비면
     `/api/stats`·`--status` 가 양쪽 토큰을 찍어 말한다. 서버가 이미 아는 값이다.
   - **남은 것 ② — 사람 쪽 권한이 Confluence 와 자동으로 안 맞춰진다.** 페이지 쪽은
     `wikilens acl` 이 따라가지만 사용자 토큰은 운영자가 손으로 고친다. 그룹에 들어가도
     모르고, **빠져도 운영자가 지울 때까지 계속 보인다** — 낡는 방향이 "덜 보임" 이 아니라
     "더 보임" 이다. 사람이 늘거나 그룹 이동이 잦은 팀에는 이 방식이 안 맞는다.
   - **남은 것 ③ — `userKey` 는 여전히 자기주장이다.** 스페이스 권한 자체도 안 가져온다
     (누가 어느 스페이스를 보는지는 운영자가 `acl/user` 로 정한다). PAT 을 받아 신원을
     검증하는 안은 **읽기 전용 보장을 깨므로** 그대로는 안 된다 — 근거·절충안·되돌아오는
     조건은 `DECISIONS.md` D22.

2. **싱크 자동화** — 현재 `sync` → `/admin/reindex`가 수동이다. cron으로 충분:
   `wikilens sync ... && curl -XPOST .../admin/reindex` (`&&` 필수 — 실패 시
   절반만 반영되는 것을 막는다).
3. **"유용했다" 판정 개선** — 신호 하나를 더했다(2026-08-06): **서빙한 힌트를 세션이
   끝내 안 읽으면 그 힌트를 미스로 되돌린다.** 그전에는 미스가 나는 경로가 "읽고 나서
   비슷한 말로 재검색" 하나뿐이라 엉뚱한 힌트도 벌점이 없었고, 그래서 `pWrong` 이
   늘 0.0 이었다. `pWrong` 정의도 고쳤다 — 궤적 성공률이 아니라 **서빙 대비 거부율**
   이어야 손익분기 `p_hit > p_wrong/(n−1)` 의 그 값이 된다(세션 성공률은
   `sessionFailureRate` 로 분리). **집계는 질의 종류를 가리지 않는다** — 힌트는 `Gate`
   분류와 무관하게 서빙되는데 한때 집계만 `cacheable` 뒤에 있어 UNKNOWN 이 통째로
   빠졌다(마커에 안 걸리는 자연어 질의가 전부 거기다). 과소 보고 방향이었다. 실측으로 자기교정 확인: 힌트를 거부하자 같은 질의의
   `학습 힌트 1 · rel=0.57` 이 다음 검색에서 0으로 내려갔다.
   **읽기 개수와 순위도 반영했다:** 마지막 읽기 앞의 것들은 "열어보고 지나친" 것이므로
   미스로 charge 한다(1건이면 미스 0, 6건이면 5 — 확신도가 비율로 드러난다). 그리고
   `dest` 의 검색 순위를 기록해 **깊은 데서 건진 것을 강한 증거로** 센다(1위 ×1,
   2~3위 ×2, 그 아래 ×3) — 1위를 읽는 건 기본 행동이지만 아래를 고르려면 위를
   지나쳐야 한다(웹 검색 클릭 모델의 position bias). **문턱 3·6과 3배 상한은 손으로
   정한 값이다** — 방향만 확립된 것이고 이 코퍼스에서 측정하지 않았다.
   **원시 계측을 더했다**(2026-08-07): 궤적 수로는 검색 횟수를 역산할 수 없다 —
   소득 없는 검색은 궤적을 안 남기는데 그게 재검색의 첫 시도라 **과소 계상**된다.
   `/api/stats` 의 `sinceStart` 가 `queries`·`sessions`·`multiQueryRate` 를 센다
   (재기동에서 0으로 돌아간다 — 로그에 없는 값이라 재생이 불가능하다).
   그리고 **클릭 없는 종료를 실패로 세지 않는다** — IR 의 good abandonment 다.
   `abandonedWithHints` 로 따로 세고 `sessionFailureRate` 분모에서 뺀다. 힌트 단위
   벌점(`rejected`)은 그대로 — 안 읽혔다는 사실은 확실하다.
   **남은 약점:** `dest = reads.last()` 라는 전제 자체는 그대로다.
4. **저장소를 비공개 git 에 올리기** — remote 가 없어 `pip install git+URL` 분기,
   타 머신 첫 설치, git source 마켓플레이스가 **전부 미검증**이다. 팀 배포의 선결 조건.
   같은 머신의 `acme-dt` 가 `github.com/example-org` 에서 설치돼 도는 걸로 보아
   경로 자체는 실재한다 — wikilens 로 확인만 안 됐다. 올릴 때 `--sparse` 를 쓴다면
   `.claude-plugin plugin cli` 셋을 다 넣어야 한다(`cli/` 가 빠지면 `CLI_SOURCE` 가
   비어 setup 이 git URL 을 되묻는 분기로 떨어진다). 게시 후에는 플러그인 이름이
   불변이 되므로 개명은 그전에 끝낼 것 — D9.

5. **서버가 여러 대일 때 학습을 공유하기 (궤적 이벤트 전달)** — 아직 안 함.
   지금은 `StateDirLock` 이 **상태 디렉터리당 서버 하나로 막는다.** 둘을 띄우면 포스팅이
   각자의 힙에 있어 서로의 학습을 재기동 전까지 못 보고, 같은 궤적 로그에 append 하며,
   같은 질의가 어느 프로세스에 붙느냐로 답이 달라진다.

   **설계상 갈림길이 하나 있고 그게 이 항목의 요점이다.** 포스팅은 궤적 로그의 **함수**라
   기동 때 전량 재생으로 복구된다. 그런데 보통의 MQ(Rabbit·SQS)는 *새* 이벤트만 주고
   과거를 안 준다 — 복구용으로 로그가 여전히 필요해져 **정본이 둘이 된다.** 이 저장소가
   반복해서 지워온 실패 모양이 그것이다. 그래서 고른다면 **오프셋부터 재생되는 로그 구조
   브로커**(Kafka·Redis Streams)여야 하고, 그것은 `trajectories.jsonl` 을 **대체**하지
   보완하지 않는다. `FileTrajectorySink` 가 그 인터페이스의 구현체 하나가 되는 모양이다.

   **세션은 나누지 않는다.** `onQuery`·`onRead`·`onEnd` 는 세션 단위 상태라 한 프로세스에
   붙어 있어야 한다(`sessionId` 로 스티키 라우팅). 실어 보내는 것은 **확정된 궤적**뿐이고,
   그건 카운터 증가라 순서에 거의 무관하다.

   **먼저 물을 것: 여러 대가 필요한가.** 지금은 부하가 아니라 가용성 이유일 텐데 그것도
   측정된 적이 없다. 근거는 `StateDirLock` 의 KDoc(무엇이 갈리는지)과 D17(로그를 압축
   대신 관측으로 다룬 이유 — 오프셋 재생이 들어오면 그 판단도 함께 바뀐다).

6. **Windows 네이티브 지원 — 지금은 WSL 만.** 파이썬 설치 안내를 더하는 문제가 아니다.
   막히는 것이 넷이고 **서버판 사용자도 걸린다**(MCP 프록시가 파이썬이다):

   - **`python3` 라는 이름.** Windows 는 `python`·`py` 다. `.mcp.json` 의
     `"command": "python3"` 과 스킬·커맨드가 그 이름을 박아 쓴다.
   - **`wikilens_cli.sh`** — bash 래퍼. 볼트를 만들거나 갱신하는 모든 경로가 지난다.
   - **`~/.wikilens/env.sh`** — 자격증명 정본이 `source` 하는 셸 스크립트다(D10).
   - **검색 경로도** 앞에서 `python3 vault_status.py` 로 볼트를 푼다. 실제 매칭은
     Claude Code 의 `Grep` 도구라 OS 무관인데, 그 앞이 안 무관하다.

   **파이썬 설치 안내만 더하면 "Windows 에서 된다" 는 잘못된 신호가 된다** — 넷을 다
   바꾸기 전에는 README 에 지원 범위를 적어두는 것이 맞다(적어뒀다).

## 곧 닿는 한계

- **서버 `grep` 예산 — 한계를 "몇 건" 으로 적지 않는다.** 스캔 비용은 문서 수가 아니라
  **바이트**에 붙는다: 문서당 ≈ 28us(파일 고정비) + 13us/KB(JVM, 이 머신).
  한계 = 예산 / 문서당 비용이고, 평균 4KB 문서면 3초에 약 37,000건이다. rg 는 문서당
  비용이 약 3~4배 싸다. **다시 재는 장치는 `GrepScaleTest`** — 합성 볼트 두 점에서
  유도하므로 코퍼스가 없어도 돈다.

  **예전에 여기 적혀 있던 `13,921건 2.44초 → 약 17,000건 한계` 는 재현되지 않았다.**
  같은 하네스로 같은 코퍼스를 다시 재니 1.21초(문서당 86us)였고, 위 모델이 그
  코퍼스의 평균 문서 크기(3.9KB)로 예측한 80us 와 8% 안에서 맞는다. 옛 값이 어떻게
  나왔는지는 복원할 수 없다 — **코퍼스에서 한 번 재어 상수로 적어두면 이렇게 된다.**
  **더 빠르게 만드는 길은 이미 재봤고 안 했다** — 병목은 엔진이 아니라 파일당 오버헤드와
  대소문자 접기이고, 본문을 힙에 두면 60배대가 되지만 최악이 "느려짐" 에서 "OOM" 으로
  바뀐다. 분해 수치·Caffeine 기각 근거·되돌아오는 조건은 `DECISIONS.md` D21.

  **같은 결정 기록에서 틀린 인과도 하나 나왔다** — "65배와 4~6배가 갈린 것은 서버가
  ACL 목록 고정비를 똑같이 내기 때문" 이라고 적혀 있었는데, 그 고정비를 재보니 전체의
  **0.18%** 라 비를 전혀 안 바꾼다. 갈린 원인은 측정 방법(워밍 유무)이다. 재현 가능한
  값은 문서당 약 3~4배.

## 손대지 말아야 할 것

- **위키에 쓰기 금지.** 사람 링크와 기계 링크가 섞이면 정답 신호가 영구히 오염된다.
  읽기 전용은 규율이 아니라 설계로 얻는 보장이다.
- **클라이언트에 볼트 배포 금지** (서버판). 배포된 사본은 회수할 수 없어
  권한 취소가 불가능해진다. 서버가 서빙하면 매 요청 ACL이 걸린다.
- **훅 부활 금지** (서버판). 읽기가 서버를 거치므로 서버가 궤적을 직접 본다.
  로컬판에는 보낼 서버가 없으므로 훅이 무의미하다.

자세한 근거와 뒤집힌 결정들의 이력은 [`DECISIONS.md`](DECISIONS.md).
