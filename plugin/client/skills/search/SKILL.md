---
name: search
description: |
  Confluence 위키에서 문서를 찾습니다 — 팀 공용 WikiLens 서버 경유 (MCP 도구 5개).
  앵커 텍스트를 색인해 **제목에 없는 표현으로도** 찾습니다.
  팀·조직의 문서·정책·런북·온보딩·아키텍처 자료를 찾을 때, 사용자가 우리끼리만 쓰는
  말이나 축약어를 쓸 때, 팀 내부 지식을 물을 때 사용하세요 — "우리 회사 배포 절차 문서
  어디 있어", "팀 위키에서 X 찾아줘", "우리 위키에 이거 있나" 류.
  지금 열려 있는 코드베이스 자체(구조·빌드·테스트)에 대한 질문에는 쓰지 마세요.
  로컬판(`wikilens-local`)도 함께 켜져 있다면 **이쪽이 우선입니다** — 같은 위키를
  형태소 분석·앵커·학습까지 얹어 찾습니다. 서버가 응답하지 않을 때만 그쪽으로 넘어가세요.
---

# 팀 위키 검색

`wiki` MCP 도구 셋으로 팀의 Confluence를 검색합니다. 로컬에 파일이 없으므로
Read나 Grep 같은 파일 도구로는 접근할 수 없습니다.

## 반드시 search 부터

문서 제목과 사람들이 실제로 쓰는 말은 다릅니다. 제목이 `OAuth 2.0 인가 코드 흐름`인데
다들 "로그인 붙이는 법"이라고 부릅니다. 그 표현은 제목에도 본문에도 없습니다.

`search`는 다른 문서들이 각 페이지를 링크할 때 쓴 앵커 텍스트까지 색인하므로
그 격차를 메웁니다. 사용자의 표현을 **그대로** 넣으세요. 격식 있는 용어로
바꾸지 마세요 — 바꾸면 오히려 못 찾습니다.

```
search("로그인 붙이는 법")
→  * 1. [200000001] OAuth 2.0 인가 코드 흐름  (PLATFORM) rel=0.74
```

그 다음 `read(pageId)`로 본문을 가져옵니다.

### 다만 군더더기는 빼세요 — 낱말을 **바꾸지는 말고**

`어떻게`·`있나`·`보고싶은데`·`문서`·`거` 같은 말은 어느 문서에나 있어 후보만 늘립니다.
사용자가 고른 낱말은 그대로 두고 그런 말만 덜어내세요.

```
"결제 화면에서 어떤 api 타는지 정리된 거 있어?"
→  search("결제 화면 api")
```

실측(개발 코퍼스 · 질의 30개): 문장을 그대로 넣으면 23건, 군더더기를 빼면 25건을
찾습니다. **숫자가 아니라 방향만 가져가세요** — 코퍼스가 다르면 값이 다릅니다.

**빼는 것이지 옮기는 것이 아닙니다.** `결제` 를 `payment` 로 고치면 앵커 색인이
메우려던 그 격차를 스스로 되돌리는 것이라 위 규칙을 어깁니다.

**남은 낱말이 전부 흔한 말이면**(`화면`·`주문`·`관리`처럼 위키 어디에나 나오는 말)
정답이 수천 건과 같은 조건이 되어 위로 올라올 이유가 없습니다.

**판정은 결과의 내용으로 하세요.** 상위 문서들이 **서로 무관한 주제**이면 질의가
너무 흔한 것입니다 — 그 낱말이 위키 곳곳에 있어 정답이 위로 올라올 이유가 없습니다.

그때는 사용자에게 **특징적인 말 하나**를 물으세요 — 제품명·화면명·담당 팀처럼
그 위키에서만 쓰는 말. 영역(스페이스)을 안다면 `tree` 로 좁혀도 됩니다.

> 첫 줄의 `어휘 후보 N` 은 **판정에 쓰지 마세요.** 요청한 개수의 세 배로 잘린
> 값이라 질의가 흔하든 드물든 거의 같습니다.

## 도구 선택

| 상황 | 도구 |
|---|---|
| 개념·주제로 찾기 | **`search`** — 거의 항상 여기서 시작 |
| 본문 가져오기 | `read(pageId)` |
| 정확 일치가 필요 (식별자, 코드 조각, 정확한 문구) | `grep` |
| 이름은 모르고 영역(스페이스/카테고리)만 알 때, 위에서부터 훑기 | `tree` |
| **답을 확정했을 때** | `answer(pageId)` — 아래 참고 |

`grep`을 먼저 쓰지 마세요. 사용자가 쓴 표현이 문서 본문에 없어서 못 찾거나,
그 표현으로 *링크한* 다른 문서가 나옵니다.

**`grep` 에 여러 낱말을 그대로 넣지 마세요.** 매칭은 한 줄 안에서 일어나므로
"출장 신청 승인" 은 그 여덟 글자가 **그 순서 그대로 붙어** 있어야 합니다.
사용자의 어순이 문서와 같을 때만 맞고, 이으면 두 경우 다 더 찾습니다:

```
grep(pattern="출장.*승인|승인.*출장", regex=true)
```

`regex=true` 는 ripgrep 과 같은 RE2 문법입니다. 역참조(`\1`)와 전방탐색(`(?=)`)은
쓸 수 없고, 쓰면 0건이 아니라 이유가 옵니다. 대소문자는 어느 쪽이든 구분하지 않습니다.

`search`가 빈손이면 `tree`를 시도해보세요. 앵커 텍스트는 다른 문서가 링크를
걸어야 생기는데, 어디서도 링크되지 않은 '고아' 문서는 앵커가 없어 `search`로
못 찾습니다. `tree`는 링크가 아니라 Confluence 페이지 계층(상위 문서)을 따라가므로
그런 문서에도 닿습니다.

### 답을 찾았으면 `answer` 로 알려주세요

사용자에게 답을 전하기 **직전에**, `read` 로 확인한 문서 중 실제로 답이 된 것 하나를
지정합니다.

```
search("사업자 회원 정책") → read(167175834) → read(176055320) → answer(167175834)
                                                                  ↑ 실제로 답이 된 것
```

**부르지 않으면 서버가 "마지막으로 read 한 문서"를 답으로 간주합니다.** 그것이 확인용
이나 배제용으로 연 문서일 때 틀린 것을 학습합니다 — 실측으로 **문서를 2개 이상 읽은
경우 3건 중 2건이 어긋났습니다.**

- **답을 못 찾았으면 부르지 마세요.** 못 찾았다는 사실도 신호입니다.
- **`read` 로 연 문서만** 지정할 수 있습니다. 아니면 기록되지 않고, 그때도 답 자체는
  그대로 사용자에게 전하세요 — 이 호출은 기록만 남기고 결과를 바꾸지 않습니다.
- `grep`·`tree` 로 찾은 것은 대상이 아닙니다(그쪽은 궤적을 안 남깁니다).


### 다 해봐도 없으면 — 거기서 멈추지 마세요

이 스킬은 위키를 뒤지는 방법이지 답할 수 있는 범위가 아닙니다. 위키에 없다고
사용자의 질문에 답할 수 없는 것이 아닙니다.

1. 위키에서 못 찾았다고 **분명히 말하세요.** 지어내지 마세요
2. 그리고 원래 질문에 **일반 지식으로 계속 답하세요**
3. 조직 고유 사정이라 일반 지식으로 못 답하면, 어디를 보면 될지 제안하세요

결과가 계속 비면 문서가 없는 게 아니라 설정이 막힌 것일 수 있습니다 —
아래 "도구가 오류를 내면"의 `--status` 를 돌려보세요.

## 결과 읽는 법

- `*` = 어휘 검색과 학습 신호 양쪽에서 나옴. 가장 신뢰도 높음
- `S` = 다른 사람들의 탐색 이력에서만 나옴
- `rel=0.74` = 과거 세션에서 이 질의에 이 문서가 유용했던 정도

여러 후보가 나오면 `*` 표시와 `rel`이 높은 쪽이 대개 정본입니다.

## 도구가 오류를 내면

주소·식별자·서버·색인 중 무엇이 막혔는지는 진단 한 번으로 갈립니다. 추측하지 말고
실행한 뒤 그 출력을 근거로 안내하세요:

```
Bash: python3 "${CLAUDE_PLUGIN_ROOT}/mcp/wikilens_mcp.py" --status

Windows 에서 Bash 도구가 없으면(Git for Windows 미설치) PowerShell 로:
`python "$env:CLAUDE_PLUGIN_ROOT\mcp\wikilens_mcp.py" --status`
```

**`ACL_USERS` 는 혼자 보면 안 됩니다 — `ACL_ENFORCED` 와 함께 읽으세요.**

| ACL_ENFORCED | ACL_USERS | 뜻 |
|---|---|---|
| `no` | 0 | **정상입니다.** 권한 확인이 꺼져 있어 등록이 필요 없습니다(기본값) |
| `yes` | 0 | 운영자가 아직 아무도 등록하지 않았습니다 — 사용자가 할 수 있는 일이 없으니 운영자에게 문의하라고 알려주세요 |
| `yes` | N>0 | 등록은 됐습니다. 그래도 0건이면 `ACL_TOKEN_OVERLAP=0` 줄이 있는지 보세요 — **문제일 때만 찍히며**, 그 아래 양쪽 토큰을 함께 보여줍니다 |

`ACL_ENFORCED=no` 인데 결과가 비면 원인은 다른 데 있습니다. `INDEXED_DOCS` 가 0인지,
`VAULT_AGE` 가 큰지를 보세요. **`--status` 는 문제가 있을 때만 찍는 줄이 있으므로,
안 보이는 키를 찾지 말고 출력에 있는 것만 근거로 삼으세요.**

## 주의

**권한 범위입니다.** 사용자가 볼 수 없는 문서는 검색 결과에도 나오지 않고
`read`도 "찾을 수 없음"으로 응답합니다. 못 찾았다고 보고할 때 이 점을 감안하세요.

**위키를 수정할 수 없습니다.** 읽기 전용입니다. 내용을 고쳐야 하면 Confluence에서
직접 하도록 안내하세요.
