# 빌드 컨텍스트는 데몬으로 통째로 전송된다. 필요한 것만 남긴다.

# 산출물·캐시 — 이미지 안에서 다시 만든다. 넣으면 호스트 JDK 로 만든 클래스가
# 컨테이너 JDK 것과 섞인다.
build/
.gradle/

# **볼트 심링크.** `mirror-root` 는 ~/wiki 로 가는 링크다(gitignore 대상).
# 컨텍스트에 들어가면 위키 본문 전체가 데몬으로 전송되고, 최악의 경우 이미지에 구워진다.
mirror-root
.wikilens/

# 로컬 실행 흔적
*.log
